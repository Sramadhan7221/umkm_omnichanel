# CLAUDE.md — umkm_omni_web

Memori jangka panjang untuk semua sesi kerja di project ini. Baca file ini di awal setiap sesi sebelum mulai coding.

## Konteks Proyek

`umkm_omni_web` adalah sistem omnichannel MSME (Shopee, GrabFood, GoFood, TikTok Shop) yang saat ini sudah punya Order Inbox, inventory sync, fee reconciliation, dan expense tracking manual. Project sedang menambahkan modul akuntansi double-entry penuh berbasis SAK EMKM (Chart of Accounts, jurnal otomatis, HPP, PPh Final, Neraca) dan modul POS offline untuk toko fisik, menggantikan financial engine ringkas yang ada sekarang. Sumber requirement: `docs/Analisis_Kebutuhan_Modul_Akuntansi_POS.pdf` (v2, 12 Agustus 2026).

## Keputusan Scope yang Sudah Final (jangan tanya ulang, jangan asumsikan beda)

1. **Double-entry MENGGANTIKAN laporan lama, bukan berdampingan.** `get_income_statement()` dan `get_cash_flow()` di `financial_service.py` akan di-deprecate setelah engine berbasis saldo akun (Epic F) tervalidasi. `financial.py` (router) dan `financial.html` diarahkan ke engine baru — jangan pertahankan sebagai mode kedua/opsional.
2. **Tidak ada backfill jurnal retroaktif.** Aplikasi belum go-live, start from zero. Database demo (OmniOrder, Settlement yang sudah ada) boleh di-reset sebelum Epic A/B mulai dipakai. Jangan bangun job migrasi data historis.
3. **Master Barang adalah single source of truth harga di semua kanal, termasuk POS Kasir.** Harga jual + HPP ditentukan owner di Master Barang, lalu disinkronkan keluar ke tiap platform. Kasir POS tidak boleh input/override harga manual — selalu ambil read-only dari Master Barang.
4. ~~**Kas fisik terpisah per outlet, tapi Neraca tampilkan baris konsolidasi.**~~ **DIBATALKAN oleh Customer Request 1 (lihat bagian di bawah).** Skema multi-outlet (Epic H) dihapus total — kembali ke satu toko implisit. Baris ini dipertahankan di sini sebagai catatan sejarah keputusan, BUKAN aturan aktif — jangan bangun ulang konsep outlet berdasarkan baris ini.

## Epic & Urutan Pengerjaan (dependency-aware)

Urutan sprint: **A + H dulu → D → B → C → E + F → G**. Semua epic P0 (A, H, D, B, C) harus selesai sebelum P1/P2.

| Epic | Nama | Prioritas | Dependency | File utama |
|---|---|---|---|---|
| A | Chart of Accounts — model & seed 30 akun | P0 | Tidak ada (fondasi) | `app/models/db_models.py` (baru: `Account`), `app/models/canonical.py`, baru: `app/services/chart_of_accounts_service.py` |
| H | Multi-Outlet & Konsolidasi Kas Fisik | P0 | — (sejalan dengan A, prasyarat C) | `app/models/db_models.py` (baru: `Outlet`), `app/models/canonical.py`, baru: `app/services/outlet_service.py`, `app/routers/outlets.py`, `app/templates/outlets.html` |
| D | Master Barang & HPP — source of truth harga | P0 | Tidak blocking A/H, tapi logically setelahnya | `app/models/db_models.py` (kolom `cogs_price`, `unit_label` di `Product`), `app/mock_adapters/base.py` + semua `mock_*.py` (method `push_price_update`), `app/services/inventory_service.py`, `app/templates/product_form.html` |
| B | Journal Engine — mapping matrix & auto-posting | P0 | Butuh A (akun) & D (HPP untuk jurnal COGS) | `app/models/db_models.py` (baru: `JournalEntry`, `TransactionMappingRule`), `app/services/financial_service.py`, `app/services/order_service.py`, `app/services/reconciliation_service.py`, `app/routers/financial.py`, `app/templates/financial.html` |
| C | POS Offline — Form Nota Kasir | P0 | Butuh H (outlet), D (harga read-only), B (jurnal) | `app/models/canonical.py` (`Channel.OFFLINE_POS`), `app/models/db_models.py`, baru: `app/routers/pos.py`, `app/services/pos_service.py`, `app/templates/pos.html` |
| E | PPh Final UMKM 0,5% | P1 | Butuh B (posting jurnal) | `app/services/financial_service.py`, `app/routers/financial.py`, `app/templates/financial.html` |
| F | Neraca & Laba Rugi dari saldo akun (ganti engine lama) | P1 | Butuh A, B, H (konsolidasi kas) | baru: `app/services/balance_sheet_service.py`, `app/services/financial_service.py` (deprecate fungsi lama), `app/routers/financial.py`, baru: `app/templates/balance_sheet.html` |
| G | Retur & Pembatalan — efek jurnal & stok | P2 | Butuh B (jurnal retur) | `app/services/order_service.py`, `app/services/financial_service.py`, `app/routers/api.py`, `app/templates/order_inbox.html` |

## Referensi Data Penting (jangan ditebak ulang, baca dari file)

- `docs/chart_of_accounts.csv` — 30 baris Chart of Accounts (dibuat di Epic A).
- `docs/transaction_mapping_matrix.csv` — 32 baris aturan jurnal per event x platform (dibuat di Epic B).

Kedua file ini adalah **sumber kebenaran**. Jangan hardcode ulang kode akun, nama akun, atau aturan debet/kredit dari ingatan atau tebakan — selalu baca langsung dari CSV tersebut sebelum menulis seed script atau mapping rule.

## Konvensi Kode yang Sudah Ada di Project

Hasil observasi `app/models/db_models.py`, `app/models/canonical.py`, `app/mock_adapters/base.py`, `app/services/*.py`:

- **Model SQLAlchemy** (`app/models/db_models.py`): satu file datar berisi semua model, dikelompokkan per modul dengan comment-block header (mis. `# --- Inventory Sync (Fase N) --- #`). Primary key business-meaningful dipakai sebagai string PK bila memang natural (`OmniOrder.platform_order_id`, `Product.sku`, `Platform.code`) — bukan selalu autoincrement id. Model baru (`Account`, `Outlet`, `JournalEntry`, `TransactionMappingRule`) ikuti pola ini: tambahkan di file yang sama dengan comment-block header baru, bukan file terpisah per model.
- **Canonical layer** (`app/models/canonical.py`): semua enum kanonik (`Channel`, `FulfillmentType`, `OrderStatus`, `FeeCategory`) didefinisikan sebagai `str, Enum` di sini, dan seluruh sistem downstream (services, routers) hanya boleh bergantung pada tipe canonical ini — jangan sentuh raw platform payload di luar `mock_adapters/`. Menambah `Channel.OFFLINE_POS` (Epic C) dan pemetaan channel → kode akun pendapatan (Epic A) masuk ke file ini.
- **PlatformAdapter pattern** (`app/mock_adapters/base.py`): abstract base class dengan method abstrak (`authenticate`, `fetch_orders`, `normalize_order`, `push_stock_update`, `parse_webhook`, `parse_settlement`). Method baru `push_price_update(sku, price, cogs_price)` (Epic D) harus ditambahkan sebagai abstractmethod di `base.py`, lalu diimplementasikan di keempat mock adapter (`mock_shopee.py`, `mock_grabfood.py`, `mock_gofood.py`, `mock_tiktokshop.py`) mengikuti pola persis `push_stock_update` (return `SyncResult`).
- **Services** (`app/services/*.py`): fungsi-fungsi flat (bukan class) yang menerima `db: Session` sebagai parameter pertama, docstring modul di atas menjelaskan fase/konteks bisnis. Ikuti pola ini untuk service baru (`chart_of_accounts_service.py`, `outlet_service.py`, `pos_service.py`, `balance_sheet_service.py`).
- **Audit trail**: pola `ProductAuditLog` (JSON `changed_fields` + `note` + `changed_time`) dipakai ulang untuk mencatat riwayat perubahan harga di Epic D — jangan buat mekanisme audit baru.

## Aturan Kerja per Sesi

- Selalu gunakan plan mode dulu untuk epic besar (B, F) sebelum menulis kode.
- Setiap epic dikerjakan di sesi/task terpisah — jangan digabung dalam satu sesi.
- Untuk fitur yang menyentuh angka jurnal/HPP/pajak, tulis test acceptance criteria dulu sebelum implementasi (contoh: total debet = total kredit per batch posting, Aset = Kewajiban + Ekuitas di Neraca).
- Sebelum menghapus kode lama (`get_income_statement`, `get_cash_flow`), wajib ada sanity-check yang membandingkan output lama vs baru pada dataset yang sama — baru hapus setelah cocok.

## Progress

- **Epic A — selesai.** `Account` model, `chart_of_accounts_service.py` (seed dari `docs/chart_of_accounts.csv` + `list_accounts_grouped`), endpoint `GET /api/financial/accounts`, `tests/test_chart_of_accounts.py` (3 test, lulus semua).
- **Epic H — fondasi selesai, laporan saldo menyusul.** `Outlet` model (`kode_outlet`, `nama_outlet`, `alamat`, `is_active`), `outlet_id` nullable di `OmniOrder`, `app/services/outlet_service.py` (seed 2 outlet demo + list/create/toggle), router `app/routers/outlets.py`, halaman `/outlets`, `tests/test_outlets.py` (9 test, lulus semua). **Belum dikerjakan:** laporan "Kas per Outlet" dengan saldo real dan baris konsolidasi di Neraca — keduanya butuh `JournalEntry` dari Epic B yang belum ada. Begitu Epic B selesai, wire SUM(debet)-SUM(kredit) akun 1111 dikelompokkan per `outlet_id` ke `outlet_service.py`, ganti catatan placeholder di `outlets.html`.
- **Epic D — selesai.** `Product.cogs_price` (HPP) + `Product.unit_label` (Satuan), keduanya wajib diisi lewat form Tambah/Edit Produk dan endpoint (`Form(...)` tanpa default). `push_price_update` ditambahkan ke `PlatformAdapter` (`app/mock_adapters/base.py`) dan diimplementasikan di keempat mock adapter, dipanggil dari `_push_price_to_platforms()` (`app/services/inventory_service.py`) setiap kali harga jual atau HPP berubah (create maupun edit). Perubahan HPP/Satuan masuk `ProductAuditLog` lewat `_TRACKED_FIELDS` yang sudah ada. `tests/test_inventory_hpp.py` (7 test, lulus semua). Katalog demo (`_INITIAL_CATALOG`) diberi HPP + satuan realistis.
- **Epic B — selesai.** `docs/transaction_mapping_matrix.csv` (32 baris, ditranskrip persis dari sheet 'Matriks Transaction Mapping' di `docs/Salinan dari SISTEM PROTOTYPE AKUNTANSI UMKM DIGITAL TERPADU.xlsx` yang user tambahkan pertengahan sesi ini — sebelumnya file Excel ini tidak ada di repo). Model baru `TransactionMappingRule` (PK `no`, seed dari CSV via `journal_engine_service.seed_mapping_rules`) dan `JournalEntry` (PK `no_jurnal`, selalu satu baris debet + satu baris kredit nominal sama, jadi "total debet = total kredit" otomatis by construction). `app/services/journal_engine_service.py`: `post_journal()` primitive + event functions `post_order_completed_journal`, `post_order_refunded_journal`, `post_settlement_journal`, `post_expense_journal`. Hook points: `api.py::_ingest_order` (order Selesai/Dikembalikan), `reconciliation_service.generate_settlements` (payout masuk bank), `financial_service.add_expense` (biaya operasional, sekarang wajib pilih `rule_no` dari 4 aturan #26/#30/#31/#32 lewat dropdown "Jenis Beban", bukan tebak dari kategori teks bebas). `CHANNEL_RECEIVABLE_ACCOUNT` ditambahkan ke `canonical.py` (pola sama dengan `CHANNEL_REVENUE_ACCOUNT` Epic A).
  **Keputusan PO (dikonfirmasi sesi ini):** 32 baris matriks sumber hanya tulis literal untuk fee/retur e-commerce (rule #4/#6/#19 eksplisit sebut 1121/Shopee-Tokopedia-TikTok); PO memilih **memperluas pola rule tsb ke GrabFood/GoFood** (kredit ke 1123 alih-alih 1121) daripada membiarkan fee Logistik/Biaya Layanan/Subsidi Promo dan retur food-delivery tidak terjurnal. `rule_no` tetap dicatat untuk traceability meski akun kredit dihitung, bukan disalin literal dari baris itu.
  UI: kartu "Jurnal Transaksi (Otomatis)" baru di `/financial` (endpoint `GET /api/financial/journal`), dropdown "Jenis Beban" + outlet (khusus rule #31, satu-satunya yang menyentuh akun outlet-scoped 1111) di form Catat Biaya (endpoint `GET /api/financial/expense-rules`).
  **Belum dikerjakan (sesuai matriks, menunggu epic lain):** rule #2 (Tokopedia — channel tidak ada di app ini), #8/#9/#11 (POS/QRIS — Epic C), #15-18 (produk digital/affiliate — tidak ada sumber data), #21-23/#25/#27 (ads/packing/selisih rekonsiliasi — tidak ada trigger saat ini), #28/#29 (PPh Final — Epic E), #20 + aksi "Proses Retur" (pemulihan stok — Epic G). Semua 32 baris tetap ter-seed di tabel (memenuhi acceptance criterion), hanya belum ada hook posting.
  `tests/test_journal_engine.py` (9 test, lulus semua + 18 test lama tetap lulus, total 27).
- **Epic C — selesai.** `Channel.OFFLINE_POS` + `FulfillmentType.WALK_IN` di `canonical.py`, `CanonicalOrder.outlet_id` (baru, dipakai `upsert_from_canonical` untuk set `OmniOrder.outlet_id` secara generik). Nota dibuat lewat `app/services/pos_service.py::create_pos_sale` yang membangun `CanonicalOrder` lalu memakai ULANG pipeline order online yang sudah ada: `upsert_from_canonical` → `deduct_stock_for_order` → posting jurnal — bukan jalur terpisah. No nota format `NT-YYYYMM###` (`generate_nota_number`). Validasi: outlet harus aktif, tiap SKU harus ada di Master Barang dengan stok cukup (reject oversell, beda dari `deduct_stock_for_order` yang clamp-ke-0 untuk sinkron mock).
  **Metode bayar POS** (`app/services/journal_engine_service.py`): rule #8 (Tunai→1111, outlet-scoped) dan #9 (QRIS→1122) adalah baris literal matriks. **Keputusan PO sesi ini:** rule #33 (Transfer→1113) dan #34 (E-Wallet→1112) ditambahkan sebagai ekstensi (via `seed_pos_payment_extension`, BUKAN dari CSV — `docs/transaction_mapping_matrix.csv` tetap persis 32 baris) karena deskripsi tugas Epic C minta 4 metode bayar tapi matriks Excel cuma tulis 2; akun yang dipakai (1113/1112) sudah ada polanya di rule lain (#7/#14/#18). `post_pos_sale_journal` menentukan `outlet_id` pada `JournalEntry` dari `Account.is_outlet_scoped` (data-driven, bukan cek string "1111" hardcoded) — cuma rule #8 yang mengisi `outlet_id`. HPP (rule #24) dipakai ulang lewat helper `_post_cogs` yang sekarang dishare antara order online dan nota POS (di-refactor dari `post_order_completed_journal`).
  `reconciliation_service.generate_settlements` sekarang exclude channel "Offline POS" — nota tidak punya konsep payout platform (rule #8/9/33/34 sudah langsung membukukan kas/bank/e-wallet saat nota disimpan).
  UI: halaman baru `/pos` (`app/templates/pos.html` + `app/static/js/pos.js`) — pilih outlet aktif dulu (form terkunci sebelum itu), pilih barang dari Master Barang (harga read-only), keranjang, metode bayar, simpan → tampilkan preview nota siap cetak (`window.print()` + CSS `@media print`) sesuai layout sheet 'Form Nota (Kasir POS)', termasuk nama+alamat outlet. Endpoint: `GET/POST /api/pos/*` (`app/routers/pos.py`) — daftar outlet & produk **tidak** diduplikasi, tetap pakai `/api/outlets` dan `/api/inventory` yang sudah ada. Link nav "Kasir POS" ditambahkan di semua 9 halaman.
  **Belum dikerjakan (gap terdokumentasi, bukan bug):** rule #10 (MDR QRIS) dan #11 (settlement QRIS ke bank) — tidak ada sumber data/trigger untuk "dana QRIS nota ini sudah cair ke bank" di epic manapun saat ini. Retur/pembatalan nota POS — Epic G.
  `tests/test_pos.py` (14 test, lulus semua + 27 test lama tetap lulus, total 41).
- **Epic E — selesai.** Rule #28 (akrual, debet 5410/kredit 2140) dan #29 (setor, debet 2140/kredit 1113) sudah ada di `TransactionMappingRule` sejak seed Epic B — epic ini murni menyambungkan ke 2 aksi baru, tidak ada baris CSV/akun/model baru. `app/services/financial_service.py`: `close_month_pph(db, period)` hitung `SUM(OmniOrder.gross_amount)` untuk order `status=="Selesai"` dalam bulan kalender `period` ("YYYY-MM"), posting nominal = 0,5% x omset bruto (bruto sebelum potongan, bukan laba bersih — sesuai kata "Omset Bruto" di rule #28 sendiri). Guard idempoten: tolak jika `sumber_dokumen` `f"Tutup Buku PPh {period}"` sudah pernah diposting (pakai konvensi teks bebas yang sudah ada, bukan kolom/tabel baru). `record_pph_deposit(db, amount, deposit_date)` posting rule #29 apa adanya sesuai nominal yang dimasukkan owner (tidak dipaksa sama dengan estimasi — sama seperti `add_expense`). `_get_account_balance(db, kode_akun, normal_saldo)` — helper saldo SATU akun dari riwayat `JournalEntry`, sengaja sempit (cuma dipakai untuk 2140 di epic ini), BUKAN engine saldo akun umum — itu tetap scope Epic F.
  UI: kartu baru "PPh Final UMKM 0,5%" di `/financial` (estimasi bulan berjalan, status Belum Ada Akrual/Belum Disetor/Sudah Disetor, tombol Tutup Buku + form Catat Setoran), endpoint `GET/POST /api/financial/pph/*`.
  `tests/test_pph.py` (7 test, lulus semua + 41 test lama tetap lulus, total 48). Diverifikasi end-to-end lewat server nyata: akrual 0,5% x 150.000 = 750, status berubah Belum Ada Akrual → Belum Disetor → Sudah Disetor setelah setor 750.
- **Epic F — selesai.** `app/services/balance_sheet_service.py` (baru): `_raw_balance` (SUM kredit - SUM debet dari `JournalEntry`, opsional dibatasi tanggal), `_GROUP_SIGN` (Aset/Beban dibalik ke arah debet, Kewajiban/Ekuitas/Pendapatan tetap arah kredit — diterapkan per **kelompok_utama**, bukan `saldo_normal` masing-masing akun, supaya akun kontra seperti 4211 Potongan Penjualan & 4212 Retur Penjualan otomatis mengurangi Pendapatan tanpa kode khusus). `get_profit_loss(db, start, end)` — Pendapatan neto, HPP dipisah dari Beban lain, Laba Kotor, Laba Bersih. `get_balance_sheet(db, as_of)` — saldo kumulatif tiap akun Aset/Kewajiban/Ekuitas sampai `as_of`, 1111 Kas di Tangan dipecah per outlet (drill-down) sekaligus dikonsolidasi jadi satu baris.
  **Keputusan PO (dikonfirmasi sesi ini):** `docs/chart_of_accounts.csv` cuma punya SATU akun Ekuitas (3110 Modal Pemilik), tidak ada "Laba Ditahan" — tanpa itu Neraca tidak mungkin balance. Neraca menampilkan baris **"Laba Ditahan (Berjalan)" yang DIHITUNG live** (akumulasi Pendapatan-Beban sejak awal), bukan akun CoA baru. **Dicatat untuk nanti, belum dibangun:** alternatif yang lebih "benar" secara formal adalah akun 3120 Laba Ditahan + mekanisme jurnal penutup tahunan — scope jauh lebih besar, di luar epic ini.
  **Migration checkpoint (sesuai permintaan PO — lihat angka konkret dulu sebelum hapus):** dijalankan side-by-side pada data demo yang sama: engine lama net_profit=Rp 2.829.040 (hitung SEMUA order apapun status, HPP tidak pernah dikurangi) vs engine baru laba_bersih=Rp 116.240 (hanya order Selesai diakui, HPP Rp146rb dikurangi eksplisit), Neraca balance sempurna (Aset=Kewajiban+Ekuitas). PO konfirmasi selisih itu perbaikan yang disengaja (bukan bug) dan setuju hapus kode lama.
  `get_income_statement`/`get_cash_flow` DIHAPUS dari `financial_service.py` beserta endpoint `/api/financial/income-statement` dan `/api/financial/cash-flow` — `financial.html` sepenuhnya pakai `/api/financial/profit-loss` sekarang (kartu Total Pendapatan/HPP/Laba Kotor/Laba Bersih + tabel Rincian Beban Operasional, menggantikan Rincian Biaya Platform/Kinerja per Platform/Arus Kas Harian yang berbasis `OmniOrderFee`/channel lama).
  Epic E's `_get_account_balance` (sempit, cuma akun 2140) DIHAPUS, `get_pph_summary` sekarang pakai `balance_sheet_service.get_account_balance` yang umum — sesuai catatan memory saat Epic E dibuat ("jangan biarkan dua implementasi saldo akun berbeda").
  **Halaman baru `/neraca`** (`balance_sheet.html`+`.js`): Aset/Kewajiban/Ekuitas terkelompok, `as_of` date picker, banner balance/tidak-balance, breakdown 1111 per outlet. Endpoint baru: `GET /api/financial/profit-loss`, `GET /api/financial/balance-sheet`, `GET /api/financial/kas-per-outlet`.
  **Menutup gap lama Epic H:** placeholder "Saldo kas per outlet akan tampil di sini setelah Journal Engine (Epic B) aktif" di `/outlets` DIGANTI tabel nyata (`outlet_service.py`'s docstring lama sudah menunjuk ke sini sejak Epic H) — pakai `get_kas_per_outlet` yang sama dengan Neraca.
  Link nav "Neraca" ditambahkan di semua 10 halaman (pola sama seperti "Kasir POS" di Epic C).
  **Belum dikerjakan (dicatat, bukan lupa):** export PDF/Excel untuk Neraca/Laba Rugi/Jurnal Transaksi (task teknis PDF menyebutnya, TAPI tidak ada di acceptance criteria — PO pilih tunda, belum ada library PDF/Excel generation terpasang di project). Akun Laba Ditahan CoA formal + siklus tutup buku tahunan (lihat keputusan di atas). Laporan arus kas harian dari `JournalEntry` (tidak dibangun ulang — tidak ada di acceptance criteria epic ini, `get_cash_flow` lama dihapus tanpa pengganti langsung).
  `tests/test_balance_sheet.py` (7 test, lulus semua + 48 test lama tetap lulus, total 55).
- **Epic G — selesai. Semua epic (A/H/D/B/C/E/F/G) sudah selesai.** Rule #19 sudah ada sejak Epic B tapi cuma jalan otomatis untuk order mock yang lahir langsung berstatus Dikembalikan — belum ada transisi status nyata dari order yang sudah Selesai. Epic G menambahkan itu: `app/services/order_service.py::process_retur(db, platform_order_id, restore_stock)` — validasi order ada, berstatus "Selesai" (bukan sudah diretur/belum selesai), lalu ubah status ke "Dikembalikan", panggil `post_order_refunded_journal` (rule #19, selalu), dan kalau `restore_stock=True` panggil `inventory_service.restore_stock_for_order` (mirror `deduct_stock_for_order`, `source="retur"`) + `post_retur_stock_recovery_journal` (rule #20, baru).
  **Gap yang diselesaikan tanpa perlu tanya PO:** rule #19 selama ini kredit ke akun piutang statis per channel (`CHANNEL_RECEIVABLE_ACCOUNT`) — tapi Offline POS (Epic C) tidak punya satu akun piutang statis, tiap nota bisa pakai metode bayar berbeda (1111/1122/1113/1112). `_pos_original_payment_account` cari `JournalEntry` asli nota itu (order_id + rule_no di `POS_PAYMENT_RULE_NOS`, pola sama persis dengan `pos_service.get_receipt`) dan kredit ke situ — bukan aturan baru, cuma prinsip "balikkan ke akun yang sama dengan penjualan aslinya" yang sudah dipakai rule #19, diselesaikan per-order karena POS tidak punya pemetaan statis per channel.
  `_compute_cogs_total` diekstrak dari `_post_cogs` supaya reversal rule #20 pakai perhitungan qty×cogs_price yang PERSIS SAMA dengan yang awalnya dibebankan rule #24 — dua jurnal itu dijamin selalu sama angkanya.
  Endpoint: `POST /api/order/{platform_order_id}/process-retur` body `{restore_stock: bool}` (`app/routers/api.py`) — tidak ada percabangan channel di router, semua logic per-channel ada di `post_order_refunded_journal`, jadi endpoint yang sama otomatis bekerja untuk order online maupun nota POS.
  UI: tombol "Proses Retur" (+ checkbox "kembalikan ke stok") muncul di modal detail Order Inbox, HANYA untuk order berstatus Selesai — bukan dropdown ubah status generik, sesuai task teknis epic ("bukan cuma ubah status").
  **Belum dikerjakan (di luar acceptance criteria, dicatat):** retur sebagian/per-item (cuma retur satu order penuh), pembalikan akrual PPh Final kalau order yang diretur sudah kepakai di bulan yang sudah ditutup buku (Epic E's `close_month_pph` sudah idempoten per periode, tidak otomatis re-kalkulasi kalau ada retur setelahnya).
  `tests/test_retur.py` (9 test, lulus semua + 55 test lama tetap lulus, total 64). Diverifikasi end-to-end: retur GrabFood tanpa restock (kredit 1123, tidak ada rule #20), retur nota POS QRIS dengan restock (kredit 1122 — bukan 1121/1123 — dan rule #20 nominal persis sama dengan rule #24 aslinya, stok naik kembali), retur ganda ditolak 400.
- **Task Backlog 1 (Rule #25 ke dropdown Jenis Beban) — selesai.** `EXPENSE_RULE_NOS` di `journal_engine_service.py` sekarang `[25, 26, 30, 31, 32]`. Validasi "wajib outlet" dibuat data-driven: `add_expense()` (`financial_service.py`) sekarang lookup `TransactionMappingRule.kode_kredit` → `Account.is_outlet_scoped` dan `raise ValueError` (ditangkap router jadi HTTP 400) kalau rule itu outlet-scoped tapi `outlet_id` kosong — bukan cek nomor rule hardcoded. Endpoint `GET /api/financial/expense-rules` sekarang menyertakan field `outlet_required` per rule (dihitung dari `kode_kredit` yang sama, bukan `kode_debet` — akun outlet-scoped 1111 selalu ada di sisi kredit untuk rule biaya, bukan debet). `financial.js` tidak lagi punya `OUTLET_RULE_NO = 31` hardcoded — `toggleOutletField()` dan validasi submit sekarang baca `expenseRulesByNo[ruleNo].outlet_required` dari response API, jadi rule outlet-scoped baru di masa depan tidak butuh ubah kode frontend.
  `tests/test_journal_engine.py` (+5 test: rule #25 posting ke akun 5120/1111 dengan outlet, rejection outlet_id kosong untuk rule #25 dan #31 (parametrized), rule non-outlet-scoped tetap boleh tanpa outlet, endpoint expense-rules mengembalikan 5 rule dengan flag `outlet_required` yang benar, endpoint POST /expenses menolak 400 tanpa outlet — total 68 test, semua lulus). Diverifikasi end-to-end lewat server nyata: POST tanpa outlet untuk rule #25 → 400 dengan pesan jelas; POST dengan outlet OUT-PST → 200, saldo Kas per Outlet OUT-PST berubah -50.000 (kredit 1111), OUT-CB1 tetap 0 — breakdown per outlet rekonsil dengan benar.
- **Task Backlog 2 (Export PDF/Excel — Neraca, Laba Rugi, Jurnal Transaksi) — selesai.** Library: `openpyxl==3.1.5` (Excel) + `reportlab==5.0.0` (PDF, bukan `weasyprint` — butuh Cairo/Pango native libs yang tidak ada di `Dockerfile` (`python:3.11-slim`, tanpa `apt-get`); reportlab pure-Python, zero Docker changes). Modul baru `app/services/export_service.py`: dua renderer generik (`_render_xlsx`, `_render_pdf`) dipakai bertiga oleh `export_balance_sheet`/`export_profit_loss`/`export_journal` — masing-masing HANYA membentuk ulang (bukan menghitung ulang) dict/list yang sudah dikembalikan `balance_sheet_service.get_balance_sheet`/`get_profit_loss`/`journal_engine_service.list_journal_entries` menjadi struktur generik `sections`/`summary`. Nilai numerik tetap numerik di xlsx (bukan string ter-format) supaya file tetap bisa dihitung ulang di Excel; PDF format ke "Rp X.XXX" saat render. Format tak dikenal (selain `pdf`/`xlsx`) `raise ValueError` → router tangkap jadi HTTP 400 (pola sama dengan `pos_service`/`financial_service`).
  Endpoint baru di `app/routers/financial.py`: `GET /api/financial/balance-sheet/export`, `/profit-loss/export`, `/journal/export` (query params identik dengan versi non-export: `as_of` atau `start`/`end`, plus `format=pdf|xlsx`) — masing-masing manggil service function yang PERSIS sama dengan endpoint JSON-nya sebelum diserahkan ke `export_service`, jadi angka di file unduhan dijamin sama dengan yang tampil di layar. Response pakai `Content-Disposition: attachment` dengan nama file mengandung tanggal/rentang.
  UI: dropdown "Export ▾" (Excel/PDF) di page-title-box Laba Rugi dan card-header Jurnal Transaksi (`financial.html`+`.js`), dan di page-title-box Neraca (`balance_sheet.html`+`.js`) — trigger lewat `window.location.href` (bukan fetch+blob, cukup karena ini GET terautentikasi cookie session same-origin, browser yang handle download-nya).
  `tests/test_export.py` (17 test baru: 6 kombinasi endpoint×format mengembalikan file valid (xlsx ke-parse ulang lewat `openpyxl.load_workbook`, pdf diawali `%PDF`), 6 kombinasi yang sama untuk periode kosong tetap 200 bukan 500, 3 test format tak dikenal → 400, 2 test angka di xlsx cocok dengan pemanggilan langsung `get_balance_sheet`/`get_profit_loss` — total 85 test, semua lulus). Diverifikasi end-to-end lewat server nyata: 6 URL export di-download lewat curl dengan session cookie asli, PDF diverifikasi magic bytes `%PDF`, xlsx di-`load_workbook` ulang dan angka `total_aset`/`total_kewajiban_ekuitas`/`laba_bersih`/`total_pendapatan` dicocokkan byte-untuk-byte dengan response JSON `/api/financial/balance-sheet` dan `/profit-loss` pada parameter periode yang sama.

## Status Proyek

Modul akuntansi double-entry + POS offline SELESAI — semua 8 epic (A, H, D, B, C, E, F, G) dari `docs/Analisis_Kebutuhan_Modul_Akuntansi_POS.pdf` sudah diimplementasikan dan diverifikasi (test otomatis + verifikasi manual lewat server nyata di tiap epic, 64/64 test lulus dikonfirmasi ulang lewat re-run independen). Kedua item Task Backlog (Rule #25, Export PDF/Excel) juga sudah selesai — lihat Progress di atas. Item yang sengaja ditunda dari 32 baris matriks/task teknis (dicatat di atas per-epic, bukan lupa): rule #2/#10/#11/#15-18/#21-23/#27 (tidak ada sumber data/trigger), akun Laba Ditahan CoA formal + tutup buku tahunan, retur sebagian/per-item. Tidak ada task backlog konkret yang tersisa saat ini.

**Catatan penting yang perlu direview manual (bukan bug, tapi keputusan bisnis yang dibuat otomatis):** migration checkpoint Epic F membandingkan net_profit engine lama (Rp 2.829.040, menghitung semua order apapun status, HPP tidak dikurangi) vs engine baru (Rp 116.240, hanya order Selesai, HPP dikurangi eksplisit) — CLAUDE.md mencatat ini "dikonfirmasi PO" tapi konfirmasi itu terjadi di sesi sebelumnya, bukan langsung dari Anda. Kalau metodologi baru ini belum pernah direview eksplisit, sebaiknya dicek dulu sebelum dianggap final.

---

# Customer Request 1 — Multi-Tenant User Management, Hapus Outlet, Forgot Password

Ditambahkan 2026-08-15. Sumber: permintaan langsung dari Syahrul (bukan dari `docs/Analisis_Kebutuhan_Modul_Akuntansi_POS.pdf`), diklarifikasi lewat 3 pertanyaan sebelum task ditulis supaya tidak menebak. Empat permintaan asli: (1) hilangkan skema multi-outlet, (2) User Management dengan role Superadmin/Owner/Admin, (3) registrasi self-service untuk Owner, (4) forgot password lewat email. Dipecah jadi 5 epic dengan urutan dependency di bawah — **ini pekerjaan paling besar di project ini sejauh ini**, lebih besar dari gabungan Epic A–G, karena mengubah app dari single-business demo menjadi multi-tenant SaaS. Baca seluruh bagian ini sebelum mulai coding di epic manapun, jangan lompat ke tengah tanpa konteks epic sebelumnya.

## Keputusan Scope CR1 — Sudah Final (dikonfirmasi user, jangan tanya ulang)

1. **Multi-tenant: data terpisah penuh per Owner.** Tiap akun Owner adalah satu bisnis UMKM independen. Produk, Order, Chart of Accounts + saldo, Jurnal, Expense, Settlement, pengaturan Platform milik satu Owner TIDAK BOLEH terlihat atau tercampur dengan Owner lain. Admin yang dibuat oleh seorang Owner beroperasi di dalam data Owner itu saja.
2. **Skema outlet dihapus total**, bukan disederhanakan jadi 1 outlet. Balik ke satu toko implisit per Owner (satu tenant = satu toko). Tidak ada model `Outlet`, tidak ada `outlet_id`, tidak ada laporan Kas per Outlet, tidak ada breakdown outlet di Neraca.
3. **SMTP untuk forgot-password pakai Mailtrap (sandbox testing).** Email TIDAK benar-benar terkirim ke inbox pengguna asli — ini disengaja untuk tahap ujicoba, bukan bug. Desain kodenya tetap harus provider-agnostic (env var host/port/username/password/from-address via `smtplib` standar) supaya ganti ke provider produksi nanti tinggal ganti `.env`, tidak ubah kode.
4. **Superadmin TIDAK punya akses ke fitur bisnis apa pun** (Produk, Order Inbox, Kasir POS, Financial, Neraca, Platforms, Reconciliation). Superadmin bukan tenant — tidak ada data bisnis yang bisa "dimiliki" Superadmin. Wewenangnya cuma 3: approve/reject registrasi Owner, dashboard jumlah+grafik Owner, nonaktifkan akun Owner. Ini keputusan eksplisit untuk menghindari ambiguitas "apakah Superadmin bisa lihat data tenant mana pun" — jawabannya TIDAK, tanpa kecuali, di iterasi ini.
5. **Admin dibatasi ke 2 kapabilitas saja: update stok, dan Kasir POS.** Secara konkret (dipetakan ke endpoint yang sudah ada): Admin BOLEH akses `GET /api/inventory`, `GET /api/inventory/{sku}/movements`, `POST /api/inventory/{sku}/adjust` (endpoint stock-adjustment yang sudah ada di `app/routers/inventory.py`), dan seluruh `/api/pos/*` + halaman `/pos`. Admin DITOLAK (403) dari: `POST/PUT /api/inventory/products*` (create/edit produk — harga & HPP tetap wewenang Owner sesuai Keputusan Scope 3 di bagian atas dokumen ini), `/api/inventory/products/{sku}/audit`, seluruh `/api/financial/*`, `/api/order/*` (Order Inbox), `/api/platforms/*`, `/api/reconciliation/*`, dan halaman-halaman terkait (`/order_inbox`, `/financial`, `/neraca`, `/reconciliation`, `/platforms`, `/inventory/new`, `/inventory/{sku}/edit`).
6. **Owner registrasi via status `pending`, bukan `is_active=False`.** Dipisah dua flag: `status` (pending/approved/rejected — hasil approval Superadmin) dan `is_active` (soft-disable, dipakai Superadmin untuk menonaktifkan Owner yang SUDAH approved, tanpa menghapus data). Login ditolak kalau `status != approved` ATAU `is_active == False`, dengan pesan error yang beda untuk tiap kasus supaya user tahu kenapa.

## Epic Baru & Urutan Pengerjaan

Urutan wajib: **I → J → K → L**, lalu **M kapan saja setelah I** (tidak bergantung ke J/K/L). Jangan kerjakan K atau L sebelum I selesai — keduanya butuh kolom `role`/`owner_id` di `User` yang dibangun di Epic I.

| Epic | Nama | Prioritas | Dependency | File utama |
|---|---|---|---|---|
| I | Fondasi User & Role (Superadmin/Owner/Admin) | P0 | Tidak ada (fondasi CR1) | `app/models/db_models.py` (`User`), `app/services/auth_service.py`, `app/routers/auth.py`, semua router yang perlu `require_role` |
| J | Registrasi Owner (self-service) + Dashboard Approval Superadmin | P0 | Epic I | baru: `app/routers/registration.py` atau perluasan `auth.py`, baru: `app/services/user_admin_service.py`, baru: `app/templates/register.html`, `app/templates/superadmin_dashboard.html` |
| K | Isolasi Data Multi-Tenant (`owner_id` di semua tabel bisnis) | P0 | Epic I (butuh `owner_id` di `User`), idealnya setelah J (supaya ada Owner sungguhan untuk uji isolasi) | `app/models/db_models.py` (`Account`, `OmniOrder`, `JournalEntry`, `Expense`, `Settlement`, `Product`, `Platform`), HAMPIR SEMUA file di `app/services/*.py` dan `app/routers/*.py` |
| L | Hapus Skema Multi-Outlet | P0 | Epic K (Account/JournalEntry sudah diubah di K, lebih aman diubah lagi sekali jalan setelahnya) | Lihat daftar 25+ file di task teknis Epic L di bawah |
| M | Forgot Password via Email (SMTP Mailtrap) | P1 | Epic I saja | `app/models/db_models.py` (baru: `PasswordResetToken`), baru: `app/services/email_service.py`, `app/routers/auth.py`, baru: `app/templates/forgot_password.html`, `app/templates/reset_password.html` |

---

### EPIC I — Fondasi User & Role (Superadmin / Owner / Admin)

**Kenapa:** Semua epic CR1 lain bergantung pada `User` punya konsep role dan kepemilikan tenant. Saat ini `User` cuma `id/email/password_hash/password_salt/created_time` — satu akun admin generik tanpa role sama sekali (`app/services/auth_service.py` baris 1-8 eksplisit bilang ini "one seeded admin account, no registration UI, no password reset flow").

**Task teknis:**
1. Tambah kolom ke `User` (`app/models/db_models.py`): `role` (String, salah satu `"superadmin"|"owner"|"admin"`, nullable=False), `owner_id` (Integer, `ForeignKey("user.id")`, self-referential, nullable — diisi HANYA untuk role `admin`, menunjuk ke `User.id` milik Owner yang membuatnya), `status` (String, default `"approved"` — lihat catatan default di bawah), `is_active` (Boolean, default True — pola sama seperti `Outlet.is_active`/`Platform.is_active`).
2. Ganti `seed_admin_user()` di `auth_service.py`: seed SATU akun `role="superadmin"`, `status="approved"`, `is_active=True` dengan kredensial default yang didokumentasikan ulang di docstring (jangan pakai email `admin@umkmapp.com` yang menyiratkan akses penuh — ganti mis. `superadmin@umkmapp.com`). Tidak ada Owner yang di-seed otomatis — Owner pertama harus lewat alur registrasi (Epic J).
3. `request.session` saat login (`app/routers/auth.py::login`) simpan tambahan `role` dan `tenant_id` (dihitung: `user.id` kalau `role=="owner"`, `user.owner_id` kalau `role=="admin"`, `None` kalau `role=="superadmin"`) — supaya request berikutnya tidak perlu query `User` ulang untuk tahu scope data.
4. Buat dependency baru `require_role(*roles)` di `auth.py`, dibangun di atas `require_login_api` yang sudah ada (401 kalau belum login, tambahan 403 kalau `request.session["role"]` tidak ada di `roles`). Pola pakai: `Depends(require_role("owner"))`, `Depends(require_role("owner", "admin"))`, dst.
5. Terapkan `require_role` ke SEMUA router sesuai pemetaan di Keputusan Scope CR1 poin 5 di atas — enumerasi lengkap endpoint per role ada di sana, jangan improvisasi pemetaan baru.
6. Endpoint baru `POST /api/auth/create-admin` (Owner-only, `require_role("owner")`): body email+password, membuat `User` baru `role="admin"`, `owner_id=<Owner yang login>`, `status="approved"` (Admin tidak perlu approval Superadmin — hanya Owner yang perlu, sesuai alur di Epic J), `is_active=True`. Endpoint ini dan UI-nya (form sederhana di halaman mana pun yang masuk akal, mis. tab baru "Kelola Tim" — putuskan lokasi UI saat implementasi, tidak kritikal).
7. Update `login()` di `auth.py`: tolak login dengan pesan berbeda untuk 3 kasus — `status=="pending"` ("Akun Anda menunggu persetujuan Superadmin"), `status=="rejected"` ("Registrasi Anda ditolak"), `is_active==False` ("Akun Anda dinonaktifkan, hubungi Superadmin") — ketiganya tetap HTTP 401, beda pesan saja.

**Acceptance criteria:**
- Login sebagai Superadmin lalu akses `GET /api/inventory` (atau endpoint bisnis apa pun) → 403.
- Login sebagai Admin lalu akses `POST /api/inventory/products` (create produk) → 403; akses `POST /api/inventory/{sku}/adjust` → 200 (kalau SKU ada).
- Login sebagai Admin lalu akses `/api/pos/sales` → 200.
- Owner yang login berhasil membuat akun Admin lewat `POST /api/auth/create-admin`; akun Admin itu tersimpan dengan `owner_id` = id Owner tsb.
- User dengan `status="pending"` gagal login dengan pesan spesifik "menunggu persetujuan"; `is_active=False` gagal login dengan pesan spesifik "dinonaktifkan".

---

**Epic I — selesai.** `User` model (`app/models/db_models.py`) mendapat 5 kolom baru: `role` ("superadmin"|"owner"|"admin"), `owner_id` (self-referential FK, hanya diisi untuk role="admin"), `status` ("pending"|"approved"|"rejected"), `is_active` (Boolean), `business_name` (nullable — disiapkan untuk form registrasi Epic J sesuai permintaan task J sendiri). **Demo DB direset** (`data/umkm_omni.db` dihapus) karena `role`/`status`/`is_active` NOT NULL tanpa default aman untuk baris admin lama — konsisten dengan keputusan project "boleh direset."
`app/services/auth_service.py`: `seed_admin_user()` sekarang seed SATU akun `role="superadmin"` (`superadmin@umkmapp.com`, bukan `admin@umkmapp.com` lagi). Tidak ada Owner yang di-seed — Owner pertama menunggu alur registrasi Epic J.
`app/routers/auth.py`: `require_role(*roles)` dibangun di atas `require_login_api` + `_get_session_role` (dua sub-dependency terpisah, bukan baca session langsung) — desain ini sengaja supaya `tests/conftest.py` bisa override kedua fungsi itu SATU KALI dan otomatis berlaku ke semua closure `require_role(...)` di 7 router berbeda (tiap router bikin closure sendiri saat import, tidak bisa di-override individual tanpa trik ini). `conftest.py`'s `client` fixture default ke `role="owner"` supaya 85 test lama tetap lulus tanpa rewrite. Endpoint baru: `POST /api/auth/create-admin` dan `GET /api/auth/admins` (keduanya Owner-only) untuk halaman baru "Kelola Tim" (`/team`, `team.html`+`team.js`, pola list+create sama seperti `outlets.html`).
Login (`POST /api/auth/login`) sekarang menolak `status=="pending"`/`"rejected"` dan `is_active==False` dengan pesan berbeda (tetap HTTP 401), dan menyimpan `role`+`tenant_id` di session. Redirect setelah login jadi role-aware (`login.html`): Owner→`/order_inbox`, Admin→`/pos`, Superadmin→tetap di halaman login dengan pesan info (belum ada dashboard, itu scope Epic J).
Router gating persis mengikuti Keputusan Scope CR1 §5: `api.py`/`financial.py`/`reconciliation.py`/`platforms.py`/`outlets.py` jadi `require_role("owner")` di level router; `pos.py` jadi `require_role("owner","admin")`; `inventory.py` baseline `require_role("owner","admin")` dengan override tambahan `require_role("owner")` khusus di 3 route (`POST /products`, `PUT /products/{sku}`, `GET /products/{sku}/audit`). Halaman (`pages.py`) pakai helper `_guard()` baru (pola sama seperti `is_logged_in` lama: redirect ke `/login` kalau belum login, tapi sekarang `HTTPException(403)` kalau role salah).
**Keputusan yang diambil tanpa tanya balik (didokumentasikan di sini, bukan ditebak diam-diam):** CR1 §5 tidak eksplisit menyebutkan halaman `/inventory` dan `/inventory/{sku}` untuk Admin — diputuskan **diizinkan** (owner+admin) karena modal "Adjust Stock" (satu-satunya kapabilitas Admin yang eksplisit diberi) secara fisik ada di kedua halaman itu; melarang halamannya akan membuat kapabilitas yang sudah diberikan tidak bisa dipakai dari UI. `outlets.py` (belum disebut CR1 karena akan dihapus total di Epic L) diperlakukan owner-only, konsisten dengan router "Pengaturan" lain.
**Nav sidebar** di-wrap `{% if role == "owner" %}` per-link (11 template — tidak ada shared partial di project ini, jadi ini pengeditan manual per file) supaya Admin tidak melihat link ke halaman yang akan 403.
`tests/test_auth.py` (9 test baru, lulus semua + 85 test lama tetap lulus tanpa perlu diubah, total 94) — Superadmin ditolak endpoint bisnis, Admin ditolak create-product tapi boleh adjust-stock & POS sales, Owner bikin Admin lewat `create-admin`, 3 varian pesan penolakan login. Diverifikasi end-to-end lewat server nyata: login superadmin→403 di `/api/inventory`, Owner dibuat manual di DB (karena Epic J belum ada)→bikin Admin lewat `/team`→Admin login→`/pos` dan `/inventory` 200, `/financial` 403, `POST /api/inventory/products` 403, dan nav sidebar `/pos` dikonfirmasi menyembunyikan "Kelola Tim"/"Kotak Masuk Pesanan"/"Laporan Keuangan" untuk Admin sementara Owner melihat semuanya.
**Belum dikerjakan (scope Epic J, bukan lupa):** halaman registrasi Owner self-service, dashboard approval Superadmin, akun Owner sungguhan (demo DB sekarang cuma punya 1 Superadmin + 1 Owner + 1 Admin buatan manual untuk verifikasi).

**Epic J — selesai.** Tidak ada kolom `User` baru (`role`/`owner_id`/`status`/`is_active`/`business_name` semua sudah ada dari Epic I). `app/services/user_admin_service.py` (baru): `register_owner` (validasi email unik, password ≥8 karakter, `business_name` wajib diisi — bikin `User` `role="owner", status="pending"`), `list_pending_owners`, `approve_owner`/`reject_owner` (harus `status=="pending"`, sekali diproses tidak bisa diproses ulang — `ValueError` kalau dicoba), `deactivate_owner`/`reactivate_owner` (state-machine guard serupa), `get_owner_stats` (jumlah Owner aktif + breakdown registrasi per bulan, dihitung DI PYTHON dengan `Counter`+`strftime`, bukan SQL `strftime`/`DATE_TRUNC` dialect-specific — biar tetap portable kalau `DATABASE_URL` diganti Postgres sesuai desain `app/database.py`).
Router baru `app/routers/registration.py` (`GET /register` publik + `POST /api/auth/register`, pola sama persis dengan `GET /login`) dan `app/routers/superadmin.py` (`prefix=/api/superadmin`, `require_role("superadmin")` di level router — `GET /stats`, `GET /owners/pending`, `POST /users/{id}/approve|reject|deactivate|reactivate`). Halaman baru: `/register` (standalone card, pola sama `login.html`) dan `/superadmin/dashboard` (`pages.py`, guard `"superadmin"` — dipakai helper `_guard()` yang sudah ada dari Epic I, tidak ada perubahan di sana) dengan sidebar MINIMAL (cuma satu item "Dashboard", karena Superadmin memang tidak punya halaman lain sesuai CR1 §4). **Tidak ada library chart ditambahkan** — tabel "Owner baru per bulan" pakai HTML table biasa, konsisten dengan seluruh app yang memang tidak pernah pakai charting library (dikonfirmasi lewat eksplorasi Epic I).
`login.html` + `auth.py`'s `GET /login`: redirect Superadmin yang sebelumnya cuma pesan placeholder (stopgap dari Epic I) sekarang benar-benar redirect ke `/superadmin/dashboard`. Link "Daftar sebagai Owner" ditambahkan di `login.html`, link balik ke "Masuk" di `register.html`.
**Keputusan PO (dikonfirmasi via AskUserQuestion sebelum coding, cross-epic gap seperti pola Epic H):** task Epic J minta approval Owner langsung men-seed 38 akun Chart of Accounts miliknya. Investigasi menunjukan ini BELUM MUNGKIN hari ini — `chart_of_accounts_service.seed_accounts(db)` masih global/sekali-jalan (`if db.query(Account).count() > 0: return`), dan `Account.kode_akun` masih primary key TANPA `owner_id` — insert ulang 38 kode yang sama untuk Owner kedua akan collide di level DB. PO pilih **tunda ke Epic K** (persis pola Epic H): `approve_owner` punya komentar `# TODO(Epic K): ...` di baris seed, tidak benar-benar memanggil apa pun.
**Temuan saat verifikasi manual yang mengoreksi asumsi rencana:** rencana awal menduga `/api/financial/accounts` akan KOSONG untuk Owner baru sampai Epic K selesai — ternyata SALAH. Karena `Account` belum py owner_id sama sekali, `list_accounts_grouped(db)` mengembalikan SATU tabel akun global yang SAMA untuk SEMUA Owner (bukan kosong, tapi shared/tidak terisolasi) — dikonfirmasi lewat curl nyata (Owner baru `newowner@example.com` langsung melihat 38 akun yang sama persis dengan Owner lain). Ini justru ilustrasi paling jelas dari masalah yang Epic K harus selesaikan (isolasi data multi-tenant), bukan bug — dicatat di sini supaya sesi Epic K berikutnya tidak kaget.
`tests/test_registration.py` (8 test baru: register→pending→gagal login, duplikat email, password pendek, stats bertambah setelah registrasi, approve→bisa login+approve ganda ditolak, reject→permanen gagal login tapi row tetap ada, deactivate→reactivate, non-superadmin ditolak semua route `/api/superadmin/*` — total 102 test, semua lulus). Diverifikasi end-to-end lewat server nyata: registrasi 2 Owner baru, satu di-approve (bisa login, masuk `/order_inbox`, lihat 38 akun global seperti dijelaskan di atas), satu di-reject (401 "ditolak" permanen, row tetap ada di DB), dashboard Superadmin menampilkan `active_owner_count` dan breakdown bulanan yang benar, dan `/register`+`/login` saling redirect dengan benar sesuai role saat sudah login.
**Belum dikerjakan (scope Epic K, bukan lupa):** CoA per-Owner sungguhan (lihat keputusan PO di atas). Isolasi data Owner lain (Product/Order/JournalEntry) — semua Owner masih melihat data global yang sama, ini memang seluruh alasan Epic K ada.

### EPIC J — Registrasi Owner (Self-Service) + Dashboard Approval Superadmin

**Kenapa:** Superadmin butuh SESUATU untuk di-approve — dan Owner butuh cara mendaftar sendiri tanpa campur tangan developer. Bergantung penuh pada kolom `status`/`role`/`is_active` dari Epic I.

**Task teknis:**
1. Halaman publik `GET /register` (tanpa login) + `POST /api/auth/register`: form minimal email, password, **nama bisnis** (field baru yang perlu disimpan — tambahkan `business_name` ke `User` di Epic I kalau belum, karena ini bagian dari identitas Owner yang dipakai di Epic K/L sebagai pengganti nama toko yang dulu ada di `Outlet.nama_outlet`). Membuat `User` baru `role="owner"`, `status="pending"`, `is_active=True`, `owner_id=None`.
2. Validasi registrasi: email harus unik (reuse constraint `User.email` yang sudah `unique=True`), password minimal policy sederhana (mis. minimal 8 karakter — dokumentasikan aturan yang dipilih di kode, jangan biarkan tanpa validasi sama sekali).
3. `GET /superadmin/dashboard` (halaman, `require_role("superadmin")`): kartu jumlah Owner aktif (`status="approved" AND is_active=True`), grafik jumlah Owner baru per bulan (group by `DATE_TRUNC`/strftime `created_time` per Owner, sederhana pakai Chart.js seperti pola grafik lain kalau ada, atau tabel kalau tidak ada precedent chart di project — cek dulu apakah ada library chart yang sudah dipakai di templates lain sebelum menambah dependency baru), dan tabel daftar registrasi `status="pending"` dengan tombol Approve/Reject.
4. Endpoint `POST /api/superadmin/users/{id}/approve`, `POST /api/superadmin/users/{id}/reject`, `POST /api/superadmin/users/{id}/deactivate`, `POST /api/superadmin/users/{id}/reactivate` — semua `require_role("superadmin")`, semua operasi pada `User.status`/`User.is_active`, TIDAK PERNAH menghapus row `User` atau data terkaitnya (sesuai permintaan eksplisit "tanpa menghapus data user owner yang tersimpan").
5. **Hook ke Epic K:** saat sebuah Owner di-approve (`status` pending→approved), trigger seed Chart of Accounts KHUSUS untuk `owner_id` itu (lihat Epic K task teknis #4) — Owner yang baru approved harus langsung punya 38 akun siap pakai, bukan kosong.

**Acceptance criteria:**
- Registrasi lewat `/register` menghasilkan `User` `status="pending"` yang GAGAL login sampai di-approve.
- Dashboard Superadmin menampilkan angka jumlah Owner aktif yang benar dan grafik yang bertambah ketika ada registrasi baru di bulan berjalan.
- Approve mengubah `status` jadi `approved` DAN memicu seed CoA untuk Owner itu (Owner bisa langsung login dan lihat 38 akun di `/api/financial/accounts` miliknya sendiri).
- Reject membuat akun permanen tidak bisa login, tapi row `User` tetap ada di database (cek langsung via query, bukan cuma "tidak error").
- Deactivate pada Owner yang sudah approved membuatnya gagal login tanpa kehilangan data historisnya (Product/Order/JournalEntry Owner itu tetap utuh di database).

---

### EPIC K — Isolasi Data Multi-Tenant

**Kenapa:** Ini jantung dari keputusan "multi-tenant, data terpisah per Owner". Tanpa epic ini, role Owner/Admin cuma teater — semua tenant tetap melihat data yang sama. **Ini epic paling berisiko dan paling luas cakupannya di seluruh project** — baca seluruhnya sebelum mulai, jangan cicil per file tanpa peta lengkap di kepala.

**Task teknis — perubahan skema:**
1. Tambah kolom `owner_id` (Integer, `ForeignKey("user.id")`, nullable=False) ke tabel: `Account`, `OmniOrder`, `JournalEntry`, `Expense`, `Settlement`, `Product`, `Platform`.
2. **`Account.kode_akun` BERHENTI jadi primary key global.** Ganti jadi: `id` (Integer, autoincrement, primary_key=True) sebagai PK baru, `kode_akun` jadi kolom `String` biasa (masih `nullable=False`), unique constraint gabungan `(owner_id, kode_akun)`. Alasan: tiap Owner punya salinan CoA sendiri dengan kode yang SAMA (mis. dua Owner sama-sama punya akun "1111"), jadi `kode_akun` sendirian tidak lagi unik di seluruh tabel.
3. **Konsekuensi wajib dari poin 2:** `TransactionMappingRule.kode_debet`/`kode_kredit` dan `JournalEntry.kode_debet`/`kode_kredit` saat ini `ForeignKey("account.kode_akun")` — FK ini TIDAK BISA dipertahankan apa adanya karena `kode_akun` bukan lagi unique. Lepas `ForeignKey` itu, jadikan `String` biasa (kode akun sebagai "label", bukan FK ter-enforce DB). Resolusi ke row `Account` yang benar dilakukan di kode aplikasi dengan query `Account.query.filter_by(owner_id=X, kode_akun=code)`, bukan `db.get(Account, code)` lagi.
4. `TransactionMappingRule` TETAP GLOBAL, TIDAK diberi `owner_id` — ini tabel konfigurasi (32+2 baris aturan) yang sama untuk semua tenant, cuma dipakai sebagai referensi kode akun abstrak saat posting jurnal.
5. Sub-tabel anak (`OmniOrderItem`, `OmniOrderFee`, `ProductPlatformMapping`, `ProductImage`, `ProductAuditLog`, `StockMovement`) TIDAK perlu `owner_id` langsung — mereka selalu dijangkau lewat parent yang sudah ter-scope (`order_id`/`sku`). **Syarat:** verifikasi dulu tidak ada query langsung ke tabel-tabel ini yang tidak melalui parent yang sudah difilter `owner_id` — kalau ketemu, entah tambahkan `owner_id` ke tabel itu juga atau perbaiki query-nya untuk join lewat parent.

**Task teknis — perubahan kode:**
6. Buat helper terpusat (mis. `app/services/tenant_context.py`) yang mengambil `tenant_id` dari `request.session` (sudah diisi di Epic I langkah 3) dan dipakai sebagai `Depends()` di setiap router — jangan duplikasi logic "ambil tenant_id dari session" di tiap file router.
7. **Setiap fungsi service yang saat ini melakukan `db.query(Model)...` tanpa filter kepemilikan HARUS ditambah filter `owner_id == tenant_id`.** Ini menyentuh praktis semua fungsi publik di: `order_service.py`, `inventory_service.py`, `financial_service.py`, `journal_engine_service.py`, `balance_sheet_service.py`, `reconciliation_service.py`, `platform_service.py`, `chart_of_accounts_service.py`. Setiap fungsi ini juga perlu terima parameter `owner_id` (bukan diam-diam baca dari suatu global state) supaya tetap mudah ditest dengan tenant berbeda-beda dalam satu test file, konsisten dengan pola `db: Session` sebagai parameter pertama yang sudah jadi konvensi project ini.
8. `chart_of_accounts_service.seed_accounts(db, owner_id)`: ubah signature untuk terima `owner_id`, jalankan PER OWNER (dipanggil dari hook approval Epic J), bukan sekali saat startup app. `journal_engine_service.seed_mapping_rules`/`seed_pos_payment_extension` TETAP jalan sekali saja saat startup (global, lihat poin 4).
9. `main.py` startup: hapus pemanggilan `seed_accounts` global kalau ada; pastikan `seed_mapping_rules`/`seed_pos_payment_extension` tetap jalan sekali.
10. Reset database demo sebelum epic ini dipakai — konsisten dengan prinsip "start from zero" yang sudah dipakai sejak Epic B, JANGAN tulis script migrasi/backfill `owner_id` untuk data lama.

**Acceptance criteria:**
- Dua Owner berbeda (`OwnerA`, `OwnerB`), masing-masing bikin Produk dan Order sendiri: `OwnerA` login lalu `GET /api/inventory` TIDAK PERNAH menampilkan SKU milik `OwnerB`, dan sebaliknya.
- `GET /api/financial/balance-sheet` untuk `OwnerA` hanya menjumlahkan `JournalEntry` milik `OwnerA` — total Aset/Kewajiban/Ekuitas `OwnerA` tidak berubah kalau `OwnerB` posting transaksi apa pun.
- Kode akun yang sama (mis. "1121") boleh punya saldo berbeda di `OwnerA` vs `OwnerB` secara independen.
- Admin yang dibuat oleh `OwnerA` (`owner_id` menunjuk ke `OwnerA`) hanya bisa adjust stok/POS di data `OwnerA`, tidak pernah bisa menyentuh SKU milik `OwnerB` sekalipun tahu SKU-nya (coba akses langsung by ID, harus 403/404, bukan cuma disembunyikan dari list).
- Seluruh test suite lama (yang sebelumnya tidak punya konsep tenant) diupdate untuk selalu menyertakan `owner_id`/tenant setup di fixture, dan tetap lulus semua.

---

**Epic K — selesai (belum di-commit, sengaja ditahan untuk direview manual dulu sesuai permintaan user sesi ini).** Ini epic paling besar sejauh ini — menyentuh skema, 9 file service, 6 router, `conftest.py`, dan 11 file test lama.
**Perluasan scope yang dikonfirmasi lewat AskUserQuestion sebelum coding (2 keputusan, keduanya bukan tebakan):**
1. Restrukturisasi PK (CLAUDE.md tertulis cuma untuk `Account`) diperluas ke `Platform` (masalah struktural identik — `Platform.code` global akan collide begitu Owner kedua di-seed). Investigasi lebih lanjut SAAT implementasi (bukan pertanyaan terpisah, perpanjangan alasan yang sama) juga menemukan `Product.sku` dan `OmniOrder.platform_order_id` punya masalah SAMA — dibuktikan konkret: `generate_nota_number` men-generate nomor sekuensial per bulan ("NT-202608001"), begitu di-scope per-owner, DUA Owner yang sama-sama transaksi pertama di bulan yang sama akan collide di PK. Jadi total **4 tabel** direstrukturisasi (`id` autoincrement + `unique(owner_id, kode_bisnis)`), bukan cuma 1 seperti tertulis di task asli: `Account`, `Platform`, `Product`, `OmniOrder`. `Settlement`/`JournalEntry`/`Expense` TIDAK direstrukturisasi (PK mereka sudah UUID-based/autoincrement, aman) — cuma ditambah kolom `owner_id`.
2. `seed_platforms`/`seed_products`/mock-order-sync/`generate_settlements` yang dulu jalan sekali di startup app dipindah ke hook approval Owner (`user_admin_service.approve_owner`) — tiap Owner baru dapat sandbox demo lengkap sendiri (akun+platform+produk+order+settlement), bukan cuma CoA. `app/main.py` startup sekarang cuma seed data global (Superadmin, 32 mapping rules, 2 rule ekstensi POS, Outlet).
**Konsekuensi skema PK restructuring:** child table yang tadinya di-join lewat business key (`OmniOrderItem.order_id`/`OmniOrderFee.order_id` ke `platform_order_id`; `ProductPlatformMapping`/`ProductImage`/`ProductAuditLog`/`StockMovement`'s `sku` ke `Product.sku`) diganti FK sungguhan ke surrogate `id` induknya (`order_id`→`omni_order.id` Integer, `sku`→`product_id`→`product.id` Integer) — bukan cuma dihapus FK-nya lalu dibiarkan cocok string. Ini ditemukan PENTING saat implementasi: relationship() SQLAlchemy (`product.mappings`, `order.items`, dst — dipakai luas di serialisasi) akan salah/bocor lintas-tenant kalau tetap join lewat `sku`/`platform_order_id` string biasa, karena begitu string itu cuma unik per-owner (bukan lagi global), dua Owner dengan SKU yang sama akan tercampur datanya lewat relationship traversal. FK ke surrogate id menghilangkan masalah ini sepenuhnya (dikonfirmasi lewat verifikasi manual: Owner A bikin SKU unik, Owner B akses langsung by SKU string → 404, bukan data campur aduk).
`Account.parent_code` (self-referencing FK) — celah yang terlewat di draft asli CLAUDE.md — juga diperbaiki dengan pola yang sama (jadi String biasa, bukan FK).
`app/services/tenant_context.py` (baru): `get_tenant_id` dependency, pola identik `require_login_api`→`_get_session_role`→`require_role` dari Epic I (fungsi kecil `_get_session_tenant_id` dipisah supaya bisa di-override terpusat di test).
**Resolver baru per model yang direstrukturisasi** (ganti `db.get(Model, kode)` yang sudah tidak valid): `chart_of_accounts_service.get_account`, `platform_service.get_platform`, `inventory_service.get_product`, `order_service.get_order` — semua `(db, owner_id, kode) -> Model | None`.
`tests/conftest.py`: `make_owner()` (bikin `User` Owner sungguhan) dan `as_tenant()` (arahkan dependency `client` ke owner tertentu) — dipakai di semua 11 file test lama + `tests/test_tenant_isolation.py` (baru, 4 test langsung mengimplementasikan 4 acceptance criteria Epic K: inventory tidak bocor, Neraca terisolasi meski kode akun sama, SKU sama boleh dipakai 2 Owner independen, Admin ditolak 404 saat coba akses SKU Owner lain langsung by ID).
`tests/test_registration.py`'s test approve butuh `seed_mapping_rules`/`seed_pos_payment_extension` manual dulu (karena fixture test tidak menjalankan lifespan app yang biasanya seed itu) — bukan bug, cuma gap test-fixture yang ditemukan & diperbaiki saat run pertama.
Total test: **106/106 lulus** (94 lama + 4 registration + 8 baru tenant-isolation, minus beberapa yang dihitung ulang karena reorganisasi — lihat masing-masing file test untuk detail).
Diverifikasi end-to-end lewat server nyata: registrasi+approve 2 Owner baru, masing-masing dapat 8 produk demo dengan kode SKU IDENTIK (SKU boleh sama karena kini unik per-owner) tapi baris DB terpisah total, Neraca masing-masing balance dengan angka independen, dan Owner A bikin produk unik (`SKU-A-ONLY`) → Owner B akses langsung by SKU → 404 (bukan 403 karena route ini gate-nya `require_role`, bukan endpoint yang mengembalikan "ditemukan tapi bukan milik Anda" — konsisten dengan pola 404-untuk-tidak-ditemukan yang sudah dipakai di seluruh app untuk resource yang tidak ada).
**Belum dikerjakan (scope Epic L, bukan lupa):** `Outlet` sengaja TIDAK di-scope ke owner_id (akan dihapus total di Epic L berikutnya, jadi tidak ada gunanya diisolasi sekarang) — berarti breakdown Kas per Outlet masih lintas-Owner secara teknis sampai Epic L menghapus konsepnya sepenuhnya. Ini didokumentasikan sebagai gap yang disengaja, bukan terlewat.

---

### EPIC L — Hapus Skema Multi-Outlet

**Kenapa:** Sesuai Keputusan Scope CR1 poin 2. Dikerjakan SETELAH Epic K supaya perubahan skema `Account`/`JournalEntry` tidak dua kali beririsan dalam sesi berbeda. Menyentuh 25+ file yang sudah teridentifikasi lewat pencarian `outlet` (case-insensitive) di seluruh `app/` sebelum task ini ditulis — daftar di bawah adalah hasil pencarian itu, bukan perkiraan.

**Task teknis — hapus:**
1. Model `Outlet` (`app/models/db_models.py`) — hapus class-nya seluruhnya.
2. Kolom `outlet_id` di `OmniOrder`, `JournalEntry`, `Expense` — hapus.
3. Kolom `is_outlet_scoped` di `Account` — hapus dari model DAN dari `docs/chart_of_accounts.csv` (baris 1111 berhenti punya kolom ini; kolom `is_outlet_scoped` sendiri dihapus dari header CSV — file ini sumber kebenaran, harus konsisten dengan skema baru).
4. File yang dihapus total: `app/services/outlet_service.py`, `app/routers/outlets.py`, `app/templates/outlets.html`, `app/static/js/outlets.js`, `tests/test_outlets.py`.
5. Registrasi router/nav: hapus `outlets` dari `app/main.py` (router include) dan hapus link nav "Kelola Outlet"/"Kas per Outlet" dari SEMUA template yang punya nav (cek satu-satu: `order_inbox.html`, `inventory.html`, `product_detail.html`, `product_form.html`, `financial.html`, `balance_sheet.html`, `reconciliation.html`, `platforms.html`, `pos.html`, dan `pages.py` route `/outlets`).

**Task teknis — ubah:**
6. `pos_service.create_pos_sale`: hapus parameter `outlet_id` dan validasi "outlet harus aktif" seluruhnya. Nota langsung dibuat tanpa pemilihan toko.
7. `journal_engine_service.post_pos_sale_journal`: hapus logic resolusi `outlet_id` berbasis `Account.is_outlet_scoped` (poin ini otomatis hilang begitu kolom `is_outlet_scoped` dihapus di poin 3 — pastikan tidak ada sisa referensi).
8. `balance_sheet_service.py`: hapus `_outlet_breakdown()`, `get_kas_per_outlet()`, dan key `breakdown_outlet` di hasil `get_balance_sheet()`. Akun 1111 Kas di Tangan jadi baris biasa seperti 1112/1113, tanpa drill-down.
9. `app/templates/pos.html` + `app/static/js/pos.js`: hapus step pemilihan outlet dari alur Kasir POS — form langsung mulai dari pemilihan barang. Layout preview nota: ganti baris nama+alamat outlet dengan `business_name` milik Owner (ditambahkan di Epic J langkah 1) sebagai nama toko di struk.
10. `app/templates/financial.html` + `app/static/js/financial.js`: hapus dropdown outlet yang sebelumnya wajib untuk rule Beban outlet-scoped (rule #31, dan #25 kalau Task Backlog itu sudah dikerjakan — cek `OUTLET_RULE_NO` di `financial.js` dan konstanta sejenis, hapus seluruhnya, bukan cuma disembunyikan).
11. Update SEMUA test yang punya assertion terkait `outlet_id`/`Outlet`: `tests/test_journal_engine.py`, `tests/test_pos.py`, `tests/test_balance_sheet.py`, `tests/test_retur.py`, dan test lain hasil Task Backlog 1 (rule #25) kalau menyentuh outlet — hapus assertion outlet-nya, JANGAN hapus seluruh test case kalau bagian lainnya masih relevan.

**Acceptance criteria:**
- Tidak ada tabel `outlet` di skema database, tidak ada kolom `outlet_id`/`is_outlet_scoped` di mana pun.
- Alur Kasir POS end-to-end (buat nota → cetak struk) tidak pernah menanyakan/menampilkan outlet.
- Neraca menampilkan satu baris 1111 Kas di Tangan tanpa breakdown apa pun.
- Full test suite lulus tanpa satu pun referensi ke `Outlet`/`outlet_id` yang tersisa (`grep -ri outlet app/ tests/` hasilnya kosong, kecuali kalau ada penggunaan kata "outlet" di luar konteks toko fisik — verifikasi manual tiap match).

---

**Epic L — selesai.** Model `Outlet` dihapus total, kolom `outlet_id` dihapus dari `OmniOrder`/`JournalEntry`/`Expense`, kolom `is_outlet_scoped` dihapus dari `Account` (model DAN `docs/chart_of_accounts.csv` — header CSV berhenti punya kolom itu). File terhapus total: `app/services/outlet_service.py`, `app/routers/outlets.py`, `app/templates/outlets.html`, `app/static/js/outlets.js`, `tests/test_outlets.py`; router `outlets` dan seed `seed_outlets` dihapus dari `app/main.py`; route halaman `/outlets` dihapus dari `pages.py` (sekarang 404).
`pos_service.create_pos_sale` tidak lagi terima parameter `outlet_id`/validasi outlet aktif — nota langsung dibuat dari pemilihan barang. `get_receipt` (dipakai preview + cetak struk) sekarang mengembalikan `nama_toko` (dari `User.business_name` milik Owner, ditambahkan di Epic J) menggantikan `outlet_nama`/`outlet_alamat` — baris alamat dihapus total dari struk karena `User` tidak punya kolom alamat. `journal_engine_service.post_pos_sale_journal` tidak lagi resolve `outlet_id` dari `Account.is_outlet_scoped` (kolom itu sendiri sudah hilang). `post_journal`/`JournalEntry` kehilangan parameter/kolom `outlet_id` sepenuhnya — `post_expense_journal` menyesuaikan.
`balance_sheet_service.py`: `_outlet_breakdown()` dan `get_kas_per_outlet()` dihapus, key `breakdown_outlet` hilang dari hasil `get_balance_sheet()` — akun 1111 Kas di Tangan sekarang baris biasa tanpa drill-down, sama seperti 1112/1113. `financial_service.add_expense` tidak lagi validasi/terima `outlet_id` (validasi itu bergantung pada `is_outlet_scoped` yang sudah dihapus). Endpoint `GET /api/financial/kas-per-outlet` dihapus; `GET /api/financial/expense-rules` tidak lagi mengembalikan field `outlet_required`.
UI: link nav "Outlet" dihapus dari 11 template (9 lewat blok `<li>` multi-baris, 2 — `product_detail.html`/`product_form.html` — lewat format satu-baris). `pos.html`/`pos.js`: langkah "Outlet Aktif" dan gating `d-none` sebelum outlet dipilih dihapus total — form kasir langsung tampil begitu halaman dibuka; preview nota pakai `nama_toko` dari respons API. `financial.html`/`financial.js`: dropdown "Outlet (kas tunai)" dan seluruh logic `ruleRequiresOutlet`/`toggleOutletField`/`loadOutletOptions` dihapus (tidak ada lagi rule beban yang butuh outlet). `balance_sheet.html`/`balance_sheet.js`: `#kas-outlet-breakdown` dan `renderKasOutletBreakdown()` dihapus.
Test: `tests/test_outlets.py` dihapus. 6 file test lain (`test_pos.py`, `test_journal_engine.py`, `test_balance_sheet.py`, `test_retur.py`, `test_auth.py`, `test_chart_of_accounts.py`) diupdate — hapus import `Outlet`, argumen `outlet_id`, dan assertion terkait; test yang isinya SELURUHNYA soal outlet (rejects-missing/inactive-outlet, per-outlet-breakdown, outlet-scoped-rule-validation) dihapus utuh, bukan cuma assertion-nya, karena tidak ada lagi perilaku lain tersisa untuk diuji. Total 87 test, semua lulus (`grep -ri outlet app/ tests/` bersih kecuali file font/CSS vendor `remixicon`/`icons.min.css` yang punya glyph ikon bernama "outlet" — soket listrik, bukan konsep bisnis, sengaja tidak disentuh).
Diverifikasi end-to-end lewat server nyata (login Owner existing di demo DB lama — skema lama masih boot karena kolom yang dihapus cuma jadi kolom nganggur, bukan hambatan `create_all`): `GET /outlets` → 404, `/pos`/`/neraca`/`/financial` tidak lagi menyebut "outlet" sama sekali, nota kasir berhasil dibuat tanpa `outlet_id` di payload dan struknya menampilkan `nama_toko` (business_name Owner) bukan nama outlet, Neraca baris 1111 tidak punya `breakdown_outlet`, dan `GET /api/financial/expense-rules` tidak lagi punya field `outlet_required`.
**Belum dikerjakan:** Epic M (Forgot Password) — lihat bagian di bawah, belum berubah dari sebelumnya.

---

### EPIC M — Forgot Password via Email (SMTP Mailtrap)

**Kenapa:** Permintaan eksplisit user. Tidak bergantung pada K/L — bisa dikerjakan kapan saja setelah Epic I selesai (butuh `User` dengan role sudah ada, tapi tidak butuh isolasi tenant maupun penghapusan outlet).

**Task teknis:**
1. Model baru `PasswordResetToken` (`app/models/db_models.py`, ikuti pola tabel audit yang sudah ada seperti `ProductAuditLog`): `token` (String, primary_key atau unique+indexed — token acak panjang, mis. `secrets.token_urlsafe(32)`), `user_id` (`ForeignKey("user.id")`), `expires_at` (DateTime), `used_at` (DateTime, nullable — diisi saat dipakai, supaya token single-use tanpa perlu hapus row, konsisten dengan prinsip "jangan hapus data" yang sudah dipakai di Epic J untuk User).
2. Service baru `app/services/email_service.py`: fungsi `send_email(to, subject, body)` pakai `smtplib` standar (tidak perlu dependency baru) + `email.mime.text.MIMEText`, baca konfigurasi dari environment variables (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `SMTP_FROM_ADDRESS`) — tambahkan variabel-variabel ini ke `.env.example` dengan nilai contoh Mailtrap (host `sandbox.smtp.mailtrap.io`, port `2525`) dan komentar yang jelas bahwa ini sandbox, bukan pengiriman email sungguhan.
3. `POST /api/auth/forgot-password` (body: email, tanpa login): cari `User` by email. **Selalu return response sukses generik yang sama** ("Kalau email terdaftar, link reset sudah dikirim") baik email ketemu atau tidak — mencegah user enumeration. Kalau ketemu DAN `status=="approved"` DAN `is_active==True`: buat `PasswordResetToken` baru (`expires_at` = sekarang + 30 menit), kirim email berisi link `/reset-password?token=...` lewat `email_service.send_email`.
4. Halaman `GET /reset-password?token=...` (tanpa login) + `POST /api/auth/reset-password` (body: token, password_baru): validasi token ada, belum dipakai (`used_at is None`), belum expired. Kalau valid: hash password baru pakai `hash_new_password` yang sudah ada di `auth_service.py`, update `User.password_hash`/`password_salt`, set `PasswordResetToken.used_at`. Token tidak valid/expired/sudah dipakai → error yang jelas, JANGAN update password.
5. Tambah link "Lupa password?" di `app/templates/login.html` menuju halaman forgot-password baru.

**Acceptance criteria:**
- Request forgot-password untuk email terdaftar & aktif mengirim TEPAT SATU email lewat SMTP yang dikonfigurasi, berisi link dengan token valid.
- Request forgot-password untuk email TIDAK terdaftar mengembalikan response yang PERSIS SAMA (status code + body) dengan request untuk email yang terdaftar — verifikasi ini secara eksplisit di test, bukan cuma "tidak error".
- Reset password dengan token valid & belum expired berhasil, lalu login dengan password lama GAGAL dan login dengan password baru BERHASIL.
- Reset password dengan token yang sama dipakai dua kali → percobaan kedua ditolak.
- Reset password dengan token kedaluwarsa (>30 menit) ditolak.
- Dicatat jelas di README/env.example bahwa Mailtrap sandbox tidak mengirim ke inbox asli — supaya tidak dikira bug saat user asli tidak menerima email.

---

**Epic M — selesai.** Model baru `PasswordResetToken` (`app/models/db_models.py`): `token` (String, `secrets.token_urlsafe(32)`, dipakai langsung sebagai PK — cukup panjang untuk aman jadi PK), `user_id` (FK ke `user.id`), `expires_at`, `used_at` (nullable, diisi saat dipakai — tidak pernah dihapus, pola sama dengan prinsip "jangan hapus data" Epic J). `app/services/email_service.py` (baru): `send_email(to, subject, body)` pakai `smtplib`+`email.mime.text.MIMEText` standar (tidak ada dependency baru), baca `SMTP_HOST`/`SMTP_PORT`/`SMTP_USERNAME`/`SMTP_PASSWORD`/`SMTP_FROM_ADDRESS` dari environment var setiap kali dipanggil (bukan di-cache saat import) — ditambahkan ke `.env.example` dengan nilai contoh Mailtrap sandbox (`sandbox.smtp.mailtrap.io:2525`) plus komentar tegas soal sandbox tidak mengirim ke inbox asli, catatan yang sama juga ditambahkan ke README.md's Notes section.
`app/routers/auth.py` (bukan router baru — 4 route baru ditambahkan di sini, mengikuti pola CLAUDE.md's file list untuk epic ini): `GET /forgot-password` + `POST /api/auth/forgot-password` (selalu balas respons generik SAMA persis — status code dan body — baik email terdaftar atau tidak, mencegah user enumeration; hanya benar-benar kirim email kalau user ditemukan DAN `status=="approved"` DAN `is_active==True`), `GET /reset-password?token=...` + `POST /api/auth/reset-password` (validasi token ada → belum dipakai → belum expired, urutan itu, sebelum validasi panjang password baru ≥8 karakter — kebijakan sama dengan `register_owner`). Reset link format `{request.base_url}reset-password?token=...` (pakai `request.base_url` bukan hardcode domain, otomatis benar di localhost maupun deploy).
UI: 2 halaman baru `forgot_password.html`/`reset_password.html`, pola sama persis dengan `register.html` (card standalone, bukan sidebar layout). `reset_password.html` baca `token` dari query string lewat `URLSearchParams` di JS, bukan lewat Jinja context — form disembunyikan total kalau token tidak ada di URL. Link "Lupa password?" ditambahkan di `login.html` di atas link "Daftar sebagai Owner" yang sudah ada.
`tests/test_forgot_password.py` (8 test baru, semua monkeypatch `email_service.send_email` lewat fixture `sent_emails` — tidak pernah menyentuh SMTP/network sungguhan): email terdaftar+aktif kirim TEPAT SATU email berisi token yang valid, respons untuk email terdaftar vs tidak terdaftar PERSIS SAMA (diverifikasi eksplisit `==` pada body JSON, bukan cuma cek "tidak error" — sesuai acceptance criteria), Owner `pending`/`is_active=False` tidak dikirimi email tapi respons tetap sama, reset dengan token valid berhasil (login password lama gagal, password baru berhasil), token dipakai dua kali → percobaan kedua ditolak 400, token kedaluwarsa (>30 menit) ditolak 400, token tidak dikenal ditolak 400, kedua halaman render 200. Total test: **95, semua lulus** (87 dari Epic L + 8 baru).
Diverifikasi end-to-end lewat server nyata: `/forgot-password` dan `/reset-password?token=xyz` render 200, `login.html` punya link "Lupa password?", `POST /api/auth/forgot-password` untuk email TIDAK terdaftar (aman dicoba lewat curl sungguhan, tidak memicu SMTP call) mengembalikan pesan generik 200, `POST /api/auth/reset-password` dengan token sampah ditolak 400 dengan pesan jelas. **Sengaja TIDAK dicoba lewat server nyata dengan email Owner terdaftar** — kredensial Mailtrap di `.env.example` cuma placeholder dan environment sesi ini kemungkinan tidak ada akses network keluar, jadi percobaan itu berisiko hang/timeout tanpa menambah keyakinan (jalur SMTP-nya sudah diverifikasi lewat automated test yang monkeypatch `send_email`). Kalau ingin verifikasi SMTP sungguhan, isi kredensial Mailtrap asli di `.env` dulu.
**Status CR1 sekarang: SEMUA 5 epic (I, J, K, L, M) selesai.** Tidak ada task CR1 lain yang tersisa.

---

## Catatan Backlog — Audit Dokumentasi User Manual (2026-08-19)

Ditambahkan lewat sesi penyusunan User Manual/materi pitching (bukan sesi coding). Audit kode aktual (`app/`) dibandingkan rencana CR1 di file ini menemukan 2 epic yang tertulis lengkap rencananya di atas TAPI belum ada baris "selesai" dan belum ada implementasinya di kode per tanggal ini — dicatat di sini supaya sesi coding berikutnya langsung tahu ini masih outstanding, bukan harus menebak ulang dari histori chat:

- ~~**Epic L — Hapus Skema Multi-Outlet: BELUM DIKERJAKAN.**~~ **Selesai sesi 2026-08-19** — lihat baris "Epic L — selesai" di bagian EPIC L di atas untuk detail lengkap. Materi pitching/User Manual boleh mencantumkan ini sebagai fitur aktif (satu toko implisit per Owner), bukan lagi "akan disederhanakan".
- ~~**Epic M — Forgot Password via Email (SMTP Mailtrap): BELUM DIKERJAKAN.**~~ **Selesai sesi 2026-08-19** — lihat baris "Epic M — selesai" di bagian EPIC M di atas untuk detail lengkap. Materi pitching/User Manual boleh mencantumkan fitur "Lupa Password" sebagai aktif, dengan catatan pengiriman email lewat sandbox Mailtrap selama masih tahap ujicoba.

Kedua item audit ini sudah selesai per 2026-08-19 — tidak ada lagi gap antara rencana CR1 di file ini dan kode aktual.

---

# Customer Request 2 — Migrasi Database ke PostgreSQL + Deployment Docker di VPS

Ditambahkan 2026-08-19. Permintaan: pindah dari SQLite ke PostgreSQL, dengan syarat eksplisit skema yang dimigrasikan harus mencerminkan struktur tabel PALING BARU (bukan versi lama/stale yang bisa gagal saat migrasi), dan kredensial database dikonfigurasi mandiri — target akhir aplikasi jalan di Docker, dideploy ke VPS sewaan. Ini murni pekerjaan infrastruktur: TIDAK ada satu kolom/tabel pun yang berubah secara bisnis, cuma storage engine dan cara migrasi dikelola yang berubah. Tidak bergantung pada epic CR1 manapun secara fungsional, tapi HARUS dikerjakan setelah CR1 karena skema yang dimigrasikan adalah skema hasil CR1 (lihat peringatan di bawah).

## EPIC N — Migrasi ke PostgreSQL + Alembic + Docker Compose untuk VPS

**Peringatan struktur "paling update" — baca ini dulu sebelum mulai apa pun:**
Satu-satunya sumber kebenaran skema adalah isi `app/models/db_models.py` DI KODE SAAT INI — bukan deskripsi tabel yang tertulis di epic-epic sebelumnya di CLAUDE.md ini (Epic A/H/B/dst di bagian atas dokumen menjelaskan RENCANA yang ditulis sebelum implementasi, dan sudah berubah signifikan sejak itu: semua tabel utama sudah punya kolom `owner_id` sejak Epic K, model `Outlet` sudah dihapus total sejak Epic L, `Account`/`OmniOrder`/`Product`/`ProductPlatformMapping`/`ProductImage`/`ProductAuditLog`/`StockMovement` sudah pindah dari primary key string ke surrogate integer `id` + `UniqueConstraint(owner_id, ...)` sejak Epic K). Kalau memakai tool autogenerate (lihat task teknis di bawah), tool itu otomatis membaca definisi class yang ada di `db_models.py` saat command dijalankan — jangan pernah menyalin ulang definisi kolom secara manual dari bagian mana pun di dokumen ini.

**Kenapa TIDAK pakai mekanisme migrasi yang sudah ada (`run_lightweight_migrations` di `app/database.py`):**
Mekanisme sekarang adalah list manual `_COLUMN_MIGRATIONS`/`_COLUMN_DROPS` yang di-maintain tangan tiap kali model berubah. Ini PERSIS jenis risiko "struktur stale" yang jadi kekhawatiran permintaan ini: kalau ada perubahan kolom yang lupa ditambahkan ke list itu, `Base.metadata.create_all()` cuma membuat tabel yang BELUM ADA sama sekali — tidak pernah mengubah tabel yang sudah ada kecuali lewat list manual tsb — sehingga database production bisa diam-diam beda skema dari `db_models.py` tanpa ada yang sadar. Ganti total dengan **Alembic** (tool migrasi standar untuk SQLAlchemy): `alembic revision --autogenerate` men-diff `Base.metadata` versi kode SAAT command dijalankan terhadap skema aktual di database, jadi migration yang dihasilkan selalu mencerminkan kode terbaru secara struktural, bukan daftar yang bisa basi.

**Task teknis — setup Alembic:**
1. Tambah ke `requirements.txt` (pin versi eksplisit, ikuti konvensi semua dependency lain di file itu yang sudah `==`): `alembic`, dan driver Postgres `psycopg[binary]` (psycopg 3 — dialect modern `postgresql+psycopg://`, bukan `psycopg2`).
2. `alembic init` untuk bikin `alembic.ini` + folder migrasi (mis. `app/migrations/`). Edit `env.py` hasil generate: (a) import `Base` dari `app.database` DAN import modul `app.models.db_models` itu sendiri (bukan cuma `Base`) — supaya semua class model ter-registrasi ke `Base.metadata` sebelum autogenerate jalan, kesalahan umum yang bikin migration kosong/tidak lengkap kalau modul model tidak pernah di-import; (b) baca `DATABASE_URL` dari environment variable yang SAMA dipakai `app/database.py` — jangan duplikasi konfigurasi koneksi terpisah.
3. Generate migration awal: `alembic revision --autogenerate -m "initial postgres schema"` — WAJIB dibaca manual file hasilnya sebelum dipakai (autogenerate tidak selalu sempurna untuk index/constraint kompleks); cocokkan tiap `CREATE TABLE`/`UniqueConstraint`/`ForeignKey` di file migration terhadap `db_models.py` satu per satu, jangan percaya mentah-mentah.

**Task teknis — lepas mekanisme migrasi lama:**
4. Hapus pemanggilan `Base.metadata.create_all(bind=engine)` dan `run_lightweight_migrations()` dari `app/main.py::lifespan` untuk jalur Postgres — startup app TIDAK LAGI membuat/mengubah skema sendiri; skema jadi tanggung jawab `alembic upgrade head` yang dijalankan terpisah SEBELUM app start (lihat poin 7).
   - Opsional (putuskan saat implementasi): boleh pertahankan `create_all()` HANYA untuk mode dev cepat SQLite tanpa Alembic, dan HANYA kalau `DATABASE_URL` berupa `sqlite://` — supaya tidak diam-diam aktif di Postgres production kalau lupa di-nonaktifkan.
5. `_COLUMN_MIGRATIONS`/`_COLUMN_DROPS`/`run_lightweight_migrations` di `app/database.py`: JANGAN dihapus diam-diam — beri komentar jelas bahwa fungsi ini digantikan Alembic sejak epic ini, dipertahankan cuma untuk opsi dev-mode SQLite di poin 4.

**Task teknis — Docker & VPS:**
6. `Dockerfile`: pastikan `alembic.ini` dan folder migrasi ikut ter-`COPY` ke image. Ubah `CMD` supaya migrasi jalan SEBELUM `uvicorn` start di setiap kali container jalan: `sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"` — app tidak boleh pernah start di atas skema yang belum ter-migrasi.
7. `docker-compose.yml`: tambah service `db` — image `postgres` versi LTS stabil terbaru saat implementasi (cek versi terkini, jangan asal pakai angka lama tanpa verifikasi), named volume untuk persist data (`postgres-data:/var/lib/postgresql/data`), healthcheck `pg_isready`. Service `web` diberi `depends_on: db: condition: service_healthy`. JANGAN expose port `5432` ke luar container di compose produksi (tidak perlu `ports:` mapping untuk `db` kecuali sedang debug lokal) — kurangi permukaan serangan di VPS publik, `web` cukup akses lewat Docker network internal.
8. **Kredensial — setting mandiri, ikuti persis (permintaan eksplisit user, jangan improvisasi pendekatan lain):**
   - Generate password acak kuat SEKALI saat setup VPS pertama kali: `openssl rand -base64 24` (atau `python3 -c "import secrets; print(secrets.token_urlsafe(24))"` kalau `openssl` tidak ada). JANGAN pernah pakai password contoh seperti "postgres"/"password123".
   - Simpan HANYA di file `.env` di server VPS (sudah masuk `.gitignore` project ini — verifikasi ulang sebelum mulai, jangan asumsikan). Isi minimal: `POSTGRES_USER`, `POSTGRES_PASSWORD` (hasil generate), `POSTGRES_DB` (mis. `umkm_omni`), lalu `DATABASE_URL=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}` — host `db` (nama service Postgres di `docker-compose.yml`), BUKAN `localhost`, karena di dalam Docker network service saling terhubung lewat nama service.
   - Update `.env.example` dengan variabel yang SAMA tapi nilai PLACEHOLDER eksplisit (mis. `POSTGRES_PASSWORD=ganti_dengan_password_hasil_generate`) — siapa pun yang clone repo tahu variabel wajib diisi, tanpa pernah melihat password asli di git manapun.
   - Tidak ada kredensial database dalam bentuk plaintext di file mana pun yang ter-commit (`docker-compose.yml`, `Dockerfile`, kode Python) — HANYA di `.env` yang gitignored.
9. Data existing SQLite: TIDAK WAJIB dimigrasikan — konsisten dengan prinsip "start from zero" yang sudah dipakai sejak Keputusan Scope 2 (Epic B). Boleh mulai kosong di Postgres, lalu `seed_admin_user`/`seed_mapping_rules`/`seed_pos_payment_extension` (tetap dipanggil seperti biasa di `lifespan` — itu data referensi, bukan skema) mengisi data awal. Kalau user secara eksplisit ingin data SQLite lama dipindahkan, itu task terpisah di luar cakupan epic ini — jangan berasumsi diminta.
10. Update bagian relevan di `README.md` (cara jalankan dengan Postgres via `docker compose up`). Jangan hapus dukungan Railway/SQLite yang sudah ada di `railway.json`/README tanpa diminta eksplisit — tambahkan opsi Postgres+VPS di sampingnya, bukan menggantikan.

**Task teknis — verifikasi (WAJIB sebelum epic ini dianggap selesai, ini bagian yang menjawab langsung syarat "jangan sampai error saat migrasi"):**
11. Jalankan `docker compose up --build` dari volume Postgres KOSONG (lingkungan bersih) — pastikan `alembic upgrade head` sukses membuat SELURUH tabel di `db_models.py` tanpa error, app start normal, halaman login bisa diakses.
12. Jalankan `alembic revision --autogenerate` SEKALI LAGI setelah `alembic upgrade head` sukses — hasilnya HARUS berupa migration kosong/no-op. Kalau autogenerate masih mendeteksi perubahan, artinya migration awal belum 100% mencerminkan `db_models.py` — perbaiki sebelum lanjut, jangan abaikan.
13. Jalankan SELURUH test suite (`pytest`) sekali terhadap Postgres sungguhan (set `DATABASE_URL` ke instance dari `docker compose up db`) sebagai smoke test tambahan — supaya masalah yang cuma muncul di dialect Postgres (constraint, tipe data, perilaku SQL yang beda dari SQLite) ketahuan sebelum deploy sungguhan ke VPS. Ini TAMBAHAN, bukan pengganti — test suite SQLite in-memory bawaan `conftest.py` tetap jadi default untuk kecepatan dev sehari-hari.
14. Uji alur end-to-end sungguhan di atas Postgres yang baru: registrasi Owner → approve Superadmin → login Owner → verifikasi CoA ter-seed otomatis (Epic J) → buat Produk → buat Order → cek JournalEntry terbentuk. Tabel yang kebentuk tapi kosong-tidak-pernah-dites TIDAK CUKUP untuk menganggap epic ini selesai.
15. Restart container tanpa `--build` (`docker compose restart` atau `docker compose down && docker compose up` tanpa `-v`) — pastikan data tetap ada (volume Postgres persist, bukan ke-reset).

**Acceptance criteria:**
- `docker compose up --build` dari environment bersih berhasil sampai app bisa diakses, tanpa satu pun error migrasi di log.
- `alembic revision --autogenerate` setelah `alembic upgrade head` menghasilkan migration kosong — bukti tidak ada drift antara skema Postgres dan `db_models.py` versi terbaru.
- Tidak ada kredensial database plaintext di file yang ter-commit ke git manapun.
- Full test suite lulus saat dijalankan terhadap Postgres sungguhan (bukan cuma SQLite in-memory).
- Alur registrasi→approval→login→CoA-seed→transaksi→jurnal berhasil end-to-end di atas Postgres.
- `app/main.py::lifespan` tidak lagi memanggil `Base.metadata.create_all()`/`run_lightweight_migrations()` untuk jalur Postgres — migrasi sepenuhnya lewat `alembic upgrade head` sebelum app start.
- Restart/redeploy container tanpa flag khusus tidak menghilangkan data (volume Postgres persist).

**File terkait:** `requirements.txt`, `app/database.py`, `app/main.py`, `Dockerfile`, `docker-compose.yml`, `.env.example`, `README.md`, baru: `alembic.ini`, folder migrasi baru (`app/migrations/env.py` + `app/migrations/versions/`).

---

**Epic N — selesai.** `requirements.txt` dapat `alembic==1.19.1` + `psycopg[binary]==3.3.4` (versi persis dicek live lewat `pip index versions`, bukan ditebak). `alembic.ini` (baru, root) + `app/migrations/env.py` (baru): `env.py` meng-import `app.models.db_models` (bukan cuma `Base`) supaya semua class ter-registrasi ke `Base.metadata` sebelum autogenerate — tanpa ini autogenerate diam-diam menghasilkan migration kosong. `sqlalchemy.url` di-set programatis dari `app.database.DATABASE_URL` (env var yang sama dipakai app, tidak ada config koneksi terpisah). Migration awal (`45535e6bdd80_initial_postgres_schema.py`) di-generate lewat `alembic revision --autogenerate` terhadap Postgres kosong sungguhan (bukan sqlite lokal), dicocokkan manual baris-per-baris terhadap 16 class di `db_models.py` (semua kolom/nullability/FK/index/unique constraint cocok persis), lalu diverifikasi `alembic revision --autogenerate` kedua kalinya menghasilkan migration kosong (no drift) — file kosong itu dihapus setelah dikonfirmasi.
**Dua keputusan diambil tanpa tanya balik (pola sama seperti epic-epic sebelumnya — extrapolasi langsung dari prinsip yang sudah dinyatakan epic ini sendiri, bukan tebakan baru):**
1. Task teknis poin 6 menulis CMD Dockerfile sebagai `alembic upgrade head && uvicorn ...` tanpa syarat — tapi poin 4 sendiri eksplisit bilang mode SQLite tetap "tanpa Alembic", dan "jangan hapus dukungan Railway/SQLite" adalah instruksi eksplisit epic ini. `Dockerfile` dipakai bersama oleh Railway (SQLite, `DATABASE_URL` belum pernah di-set di sana) dan Compose/VPS (Postgres) — menjalankan `alembic upgrade head` tanpa syarat akan CRASH deployment Railway yang sudah ada (tabel dibuat `create_all()`, tidak ada riwayat `alembic_version` untuk direkonsiliasi Alembic). `CMD` jadi bercabang lewat `case "$DATABASE_URL" in ''|sqlite*) ... uvicorn langsung ;; *) alembic upgrade head && uvicorn ;; esac`.
2. `tests/conftest.py` tidak ada di "File terkait" epic ini, tapi acceptance criterion "Full test suite lulus ... terhadap Postgres sungguhan" mustahil dipenuhi selama fixture `db`-nya hardcode `sqlite:///:memory:`. Fixture diubah baca `DATABASE_URL` dari environment kalau di-set (`Base.metadata.drop_all()` sebelum DAN sesudah tiap test, karena server Postgres persist antar test, beda dari engine in-memory sqlite yang selalu baru) — default (tanpa `DATABASE_URL`) tidak berubah sama sekali.
**Bug nyata ditemukan & diperbaiki lewat verifikasi Postgres (persis skenario yang jadi alasan poin 13 ada):** `reconciliation_service.generate_settlements` memanggil `db.add(settlement)` lalu langsung `post_settlement_journal(db, settlement)` (yang membuat `JournalEntry` dengan `settlement_id` mengacu ke `Settlement` itu) TANPA flush di antaranya. `JournalEntry.settlement_id` adalah FK yang benar-benar di-enforce DB (`ForeignKey("settlement.settlement_id")`), dan session app ini jalan dengan `autoflush=False` — jadi baris `Settlement` belum ada di DB saat `JournalEntry` di-insert. SQLite tidak pernah menangkap ini (FK constraint tidak di-enforce secara default di SQLite); Postgres menangkapnya lewat `ForeignKeyViolation` persis saat suite dijalankan terhadap Postgres sungguhan (2 test gagal: `test_approve_then_login_succeeds_and_double_approve_rejected`, `test_deactivate_then_reactivate_owner`, keduanya lewat `approve_owner` yang memanggil `generate_settlements`). Diperbaiki dengan `db.flush()` setelah `db.add(settlement)` — pola yang sama persis sudah dipakai `financial_service.add_expense` (commit `Expense` dulu sebelum `post_expense_journal`). Setelah fix: 95/95 test lulus terhadap Postgres sungguhan DAN terhadap SQLite (regresi dicek ulang).
**Bug pre-existing tak terkait yang juga diperbaiki karena memblokir acceptance criteria epic ini:** `.dockerignore` punya baris `docs/` yang mengecualikan folder itu dari build context — padahal `Dockerfile` sudah lama punya `COPY docs ./docs` (dibutuhkan runtime: `chart_of_accounts_service`/`journal_engine_service` baca CSV seed dari situ). `docker compose up --build` gagal total sebelum fix ini (`"/docs": not found`). Baris `docs/` dihapus dari `.dockerignore`. Terpisah, `.gitignore`'s pola `.env.*` ternyata juga menelan `.env.example` sendiri (`git ls-files .env.example` kosong sebelum fix) — file template yang justru dimaksudkan epic ini untuk di-commit ("siapa pun yang clone repo tahu variabel wajib diisi") tidak pernah bisa ter-track. Ditambahkan `!.env.example` setelah pola `.env.*`.
`postgres:18` (dicek live via web search — 18.6 rilis 11 Agustus 2026, 19 masih beta) ternyata mengubah konvensi mount volume resminya: mount lama `/var/lib/postgresql/data` bikin image menolak start ("these Docker images are configured to store database data in a format which is compatible with pg_ctlcluster..."); mount yang benar untuk 18+ adalah `/var/lib/postgresql` (parent dir, subfolder versi dibuat otomatis di dalamnya) — ditemukan lewat percobaan `docker compose up` langsung, bukan dari dokumentasi yang dibaca dulu.
`app/database.py`: `_COLUMN_MIGRATIONS`/`_COLUMN_DROPS`/`run_lightweight_migrations` DIPERTAHANKAN (bukan dihapus) dengan komentar baru menandai sudah digantikan Alembic, cuma dipakai untuk mode dev SQLite. `app/main.py`'s `lifespan`: `create_all()`+`run_lightweight_migrations()` sekarang di dalam `if DATABASE_URL.startswith("sqlite")` — jalur Postgres murni mengandalkan `alembic upgrade head` yang sudah jalan sebelum container ini boot. Seed global (`seed_admin_user`, `seed_mapping_rules`, `seed_pos_payment_extension`) tetap jalan tanpa syarat di kedua jalur.
`docker-compose.yml` ditulis ulang total jadi setup Postgres+VPS (bukan lagi compose SQLite lama): service `db` (`postgres:18`, healthcheck `pg_isready`, volume `postgres-data:/var/lib/postgresql`, TANPA `ports:` — cuma bisa diakses `web` lewat network Docker internal) dan `web` (`depends_on: db: condition: service_healthy`, `DATABASE_URL` dirakit dari `${POSTGRES_USER}`/`${POSTGRES_PASSWORD}`/`${POSTGRES_DB}` dengan host `db`, plus `SESSION_SECRET_KEY`/`SMTP_*` diteruskan dari `.env`). `.env.example` dapat bagian baru `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` (placeholder eksplisit + instruksi `openssl rand -base64 24`) dan `SESSION_SECRET_KEY` (ditambahkan karena `docker-compose.yml` sekarang mereferensikannya — tanpa placeholder di `.env.example`, variabel kosong di `.env` akan meng-override fallback demo key `main.py` dengan string kosong, bukan benar-benar "tidak di-set"). `README.md` dapat section baru "Deploy dengan Docker Compose + PostgreSQL (VPS)" di samping (bukan menggantikan) instruksi SQLite/Railway yang sudah ada.
**Diverifikasi end-to-end lewat lingkungan Docker sungguhan (bukan cuma dibaca/ditinjau), semua acceptance criteria:** (1) `docker compose up --build` dari volume Postgres kosong → `alembic upgrade head` sukses di log, `/login` 200. (2) `alembic revision --autogenerate` sekali lagi terhadap Postgres yang sama (lewat `docker exec` ke container `web`, bukan expose port `db`) → migration kosong (`pass`/`pass`), file dibuang. (3) Full `pytest` (95 test) dijalankan terhadap Postgres standalone terpisah (`docker run -p 5432:5432 postgres:18`, sengaja BUKAN service `db` di compose yang tidak expose port) — awalnya 2 gagal (bug settlement di atas), 95/95 lulus setelah fix, dan 95/95 tetap lulus di SQLite (regresi). (4) Alur end-to-end sungguhan lewat `curl` terhadap `web` yang jalan di compose: register Owner → login Superadmin (`superadmin@umkmapp.com`) → approve → login Owner → CoA ter-seed (5 kelompok akun) → 8 Produk demo ada → 10 Order ter-sync → 20 `JournalEntry` ter-posting, semua 200 tanpa error di log container. (5) `docker compose down` (tanpa `-v`) lalu `docker compose up` (tanpa `--build`) → login Owner yang sama + 20 `JournalEntry` yang sama masih ada persis (volume persist dikonfirmasi, bukan diasumsikan). (6) `grep` kredensial di file ter-git-track (`docker-compose.yml`/`Dockerfile`/`.env.example`) → cuma referensi `${VAR}`/placeholder teks, tidak ada password literal; `.env` sungguhan (dipakai untuk seluruh verifikasi di atas) dikonfirmasi TIDAK muncul di `git status` (ignored).
Container/volume/image Docker hasil verifikasi dan `.env` lokal sudah dibersihkan (`docker compose down -v`) setelah semua langkah di atas lulus — tidak ada state tersisa dari sesi ini di mesin dev.
**Status CR2 sekarang: Epic N selesai.** Tidak ada task migrasi Postgres lain yang tersisa dari `docs/Analisis_Kebutuhan_Modul_Akuntansi_POS.pdf` maupun permintaan Postgres/VPS ini.
